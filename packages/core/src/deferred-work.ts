/**
 * Outstanding-work registry (2026-07-21): the durable, MCP-visible record of
 * what's shipped-but-not-yet-verifiable and what's deliberately deferred, so a
 * closed ticket is never mis-read as "not done" and a deploy-timing-gated fix is
 * never mis-read as a failure. Surfaced by the `get_deferred_work` MCP tool and
 * referenced from resolutions. Update this when an item ships or is verified.
 *
 * `status`:
 *  - shipped_pending_verification: code deployed + tested, but the EFFECT only
 *    shows after a data cycle (next analytics ingest, YouTube's 24-72h lag) or
 *    needs a live check the sandbox can't run. NOT incomplete — verify the right
 *    signal, not the pre-deploy state.
 *  - deferred: intentionally not built yet (usually because it changes live
 *    production behaviour and must be enabled with the operator present).
 */

export type DeferredStatus = "shipped_pending_verification" | "deferred";

export type DeferredItem = {
  key: string;
  title: string;
  /** source ticket ULID(s) */
  ticket: string;
  status: DeferredStatus;
  summary: string;
  /** what must happen to close it out / verify it */
  nextStep: string;
};

export const DEFERRED_WORK: DeferredItem[] = [
  {
    key: "voiceover-assembly-fail-closed",
    title: "#123 the voiceover assembly re-reads the live script, and a mismatch HOLDS the run instead of generating images",
    ticket: "01M07GV0417K9TV6GFDX8XFF5A (#123)",
    status: "shipped_pending_verification",
    summary:
      "#123: two operator-narrated productions assembled tracks whose piece count disagreed with the script's segment count in OPPOSITE directions (94 pieces from 106 recorded takes — 12 takes unused, ~50s of narration missing; 26 from 25 — a piece for a sentence that had been deleted) and BOTH advanced to visuals_review, generating 74 images against the wrong audio. ROOT CAUSE, confirmed in code: edit_script_beats (#117) is allowed at the voiceover_recording gate and rewrites the draft IN PLACE, but the pipeline run is parked in waitForEvent holding the PRE-EDIT script in memory — so on approval it planned the assembly (and later the shot plan) from superseded beats. The script gate already reloads the live draft after its wait (reload-approved-script-v*); the recording gate, added later, never did. 94 and 26 are both PREVIOUS cut counts, exactly as the ticket suspected. #103's guard could not catch it: it asserts only that pieces map to DISTINCT FILES, which a stale-but-self-consistent plan does. SHIPPED all four requests: (1) FAIL-CLOSED — the finished assembly is checked (new pure core/voiceover-plan.ts checkAssemblyPlan) against the LIVE script and LIVE take set, both re-read at assembly time; on any disagreement NO voiceover asset is written and the production is held on_hold with haltKind precondition naming the counts, so no images are generated. Legitimate shapes still pass: a whole-script DAW take is 1 piece, a legacy whole-beat take collapses that beat's segments. (2) ROOT FIX — a reload-script-after-recording-gate step re-reads the live draft, so the assembly, the word timings and every shot come from the script stored now. (3) SHOT PLAN — the voiceover stamps a narrationFingerprint of the text it was cut from, re-checked before the shot plan is cut: a track built from a superseded script can never become the shots' timing source (this also covers the reuse/reopen path, where a kept voiceover predates an edit). get_production().voiceover reports assembledFromCurrentScript + scriptDriftWarning; get_production_shots/get_gate report narrationDriftShots (shots whose narration is not in the current script); edit_script_beats returns visualsStale and says plainly that existing shots do NOT follow the edit. (4) ALIGNMENT RECONCILES — the breakdown counted whisper + OPERATOR-estimated only, so TTS-filled pieces were in `pieces` and in neither bucket (the ticket's 91 + 0 vs 94: the 3 unaccounted pieces were TTS fill for segments the stale plan had no take for). It now reads {whisper, estimated, tts, pieces} with unaccounted surfaced if a piece carries an unknown value, plus a ttsFilledNote. Also from the acceptance: get_production().voiceover returns expectedDurationSec (the ticket's formula over #120's resolved read rate) and a durationWarning outside a basis-scaled band (±15% measured, ±30% on the 2.5 w/s default) — ADVISORY, not a hold, because the reported track was only ~7% short, inside any honest band for human delivery; its job is the shape the piece count cannot see (right number of pieces, wrong audio in them). En route: get_gate's per-shot narration was reading beats[shotIdx].text — the BEAT at the SHOT's index, a different line once a beat fans into several shots — and now reads the shot's own stamped narration. 25 new unit tests.",
    nextStep:
      "Operator (after the Render deploy + a connector reconnect): (1) get_production on 01KZZPGB80J21ZMBFPWDBE4BT9 and 01KZZNV2P3WSRZVQY1XN8TVBJP — voiceover.alignment should now reconcile (whisper + estimated + tts == pieces; production A should show tts:3 against pieces:94, which is where the 91-vs-94 gap went), and get_production_shots should list narrationDriftShots for the shots carrying pre-edit text (idx 22/23 on the Short, idx 48 on the long-form). Both are free reads that prove the deploy landed; the existing tracks are NOT repaired by this fix. (2) Repair them with reopen_stage('voiceover') — it re-assembles from the live script and keeps every recorded take; on the long-form all 106 takes should then be used (assembledPieces == segmentCount == 106, tts:0), and the shots re-cut from the corrected audio. (3) The fail-closed guard itself is the part the sandbox cannot exercise live (no prod DB, no stored takes, no ffmpeg run): the piece-count arithmetic, the legitimate-shape exemptions, the fingerprint and the drift detection close on the unit tests. To see the guard fire in production, edit narration at a voiceover gate and approve — a mismatched assembly now stops at on_hold/precondition with the counts in blocked.reason instead of reaching visuals.",
  },
  {
    key: "placeholder-shots-declared",
    title: "#122 an empty imagePrompt no longer reaches the image engine; a placeholder that IS served is declared everywhere",
    ticket: "01M07GGFSCXN12SCJRCV1XHD3F (#122)",
    status: "shipped_pending_verification",
    summary:
      "#122: a shot whose resolved imagePrompt was EMPTY was sent to the image engine as an empty string; the engine cannot serve it, the routing wrapper degraded to the mock backstop, and a grey PLACEHOLDER SVG landed in the shot list with shotCount/assetCounts.stills all reading correct — the operator found three across two channels by scrolling the cockpit. Two ways in: (a) a beat authored with imagePrompts[] only (the documented fan-out shape) leaves the singular imagePrompt empty, so shots past the array's end resolved to \"\"; (b) buildImagePrompts concedes a shot to its draft, and that draft is `visualBrief ?? imagePrompt` — \"\" when a beat carries neither (the clean case: 'No wreckage from that day has ever been found', a beat with no nameable subject on a real_footage channel that fell through sourcing to generation with nothing to generate from). SHIPPED all four requests: (1) new pure core/shot-prompt.ts resolveShotPrompt repairs every shot BEFORE the fan-out in the ticket's order — own prompt → beat imagePrompt → nearest sibling of imagePrompts[] → visualBrief → a narration-derived scene line — and the pipeline runs the fill_thin_prompts elaboration (buildImagePrompts, seeded with that line so a failed call still lands on it, never \"\") over the shots that reach the last resort; the repaired prompt also gets the #93 style register, since it never went through the builder. (2) placeholders are DECLARED: get_production_shots and get_gate return placeholderShots + placeholderNote, per shot placeholder/promptFallback/engineErrors; the cockpit Visuals tab shows a red Placeholder chip + a crit banner; isPlaceholderImage also catches pre-fix shots by their .svg key, so the operator's three light up without a regenerate. (3) authoring-time: shotPlan.notes names a beat that supplies imagePrompts[] while leaving the singular imagePrompt EMPTY (distinct from the #106 duplicate-image wording, which assumed it had content), plus any beat with NO visual direction at all; perBeat[] carries singularPromptEmpty. (4) the separate mechanism — a FULL prompt served by mock-media while 22 sibling shots served seedream — is a transient engine failure absorbed into the backstop: the requested engine now gets ONE RETRY before any degrade, and every engine's error rides out on the asset as engineErrors. 22 new unit tests (fallback order, placeholder detection, projection notes, the retry/degrade/declare path with fake providers).",
    nextStep:
      "Operator (after the Render deploy + a connector reconnect for the new fields): (1) get_production_shots on 01KZKC78504J1T2TGRT8H4N6CQ (Wings & Stories, idx 7), 01KZZNV2P3WSRZVQY1XN8TVBJP (idx 0 and 23) and 01KZZPGB80J21ZMBFPWDBE4BT9 (idx 49) — each should now list those idxs in placeholderShots with placeholder:true on the shot and a placeholderNote in `note`; the existing images are unchanged (detection is by .svg key), so this is the free read that proves the deploy landed. (2) Any of those still at visuals_review can be fixed in place with regenerate_shot. (3) The generation-side repair only shows on a NEW authored run: author_script a beat with imagePrompts:['a','b'], an empty singular imagePrompt and a 3-shot beat — shotPlan.notes should name the beat at authoring time, and at the visuals gate the third shot should carry promptFallback ('sibling_prompt' or 'narration') with a real image, not a placeholder. The fallback order, the projection notes and the engine retry/degrade all close on the unit tests.",
  },
  {
    key: "measured-read-rate",
    title: "#120 word_budget sized at the measured operator read rate (2.89 w/s), not the 2.5 constant",
    ticket: "01KZYEPH5YA01VX4A9A22C5ZAP (#120)",
    status: "shipped_pending_verification",
    summary:
      "#120: review_beat_map's word_budget converted targetLengthSec to words at a fixed 2.5 w/s while the operator's real rate (2.89 w/s, pooled over three fully Whisper-aligned assembled narrations) was already stored — the gate under-read them 16%, BLOCKING maps that would have hit the target and mandating ~14%-short ones (ten Dog-Eared maps were authored to the wrong budget). Shipped, all four requests: (1) core/read-rate.ts learns the rate from CLEAN samples (meta.source operator AND every assembled piece operator-recorded + whisper-aligned — tighter than the ticket's estimated:0, since a TTS fill speaks at the synth's rate; words from the newest script draft, segments = assembled piece count), evidence-gated at ≥3 samples exactly like suggestedLengthBasis. (2) Segment density: the fit models duration = words/rate + gap×segments with plausibility guards — the gap cap is deliberately ~1s because the ticket's own 3 points decompose into 4.14 w/s + 1.9s/segment (a perfect fit that would overshoot sparse maps; the solver was absorbing slower delivery into the gap), in which case the POOLED rate is the honest model — so today's data resolves to 2.89 w/s / gap 0, and a genuine sub-second gap engages as more samples land; when a gap IS fitted, the word budget subtracts a per-map pause allowance sized from that map's own estimated segment count. (3) Surfaced: review_beat_map returns readRate {wordsPerSec, segmentGapSec, basis, sampleProductions}, the word_budget evidence names the rate, get_channel_config returns the same object, and the shotEstimate uses the same rate for wordBudget→seconds (durations stay consistent with the budget). (4) Cold start: a channel below the sample floor inherits the PLATFORM-wide operator pool (basis operator_platform — assumes one narrator per instance, stated in the type docs), so Dog-Eared budgets at Wings & Stories' measured rate immediately. 11 new unit tests including the ticket's exact reproduction (168 words @55s accepted at 2.89, still blocked at default 2.5) and the fit recovering a synthetic 3.0 w/s + 0.5s gap.",
    nextStep:
      "Operator (after the Render deploy + a connector reconnect for the new readRate fields): (1) get_channel_config on Dog-Eared — Shorts 01KZNY8KRQBN819X58T7QM4E9W — readRate should read ~2.89 w/s with basis operator_platform (inherited from Wings & Stories' three clean samples). (2) Re-submit the ticket's 168-word/55s map — it should PASS word_budget, and the response's readRate should show the resolved rate. (3) On Wings & Stories itself basis should read operator_measured. The ten maps authored to the 138-word budget can be re-reviewed against the corrected band before narration is recorded. Deterministic parts (fit, gating, band math) close on the unit tests.",
  },
  {
    key: "timed-out-reviews-visible",
    title: "Review queue surfaces timed-out gates AND compliance flags — parked work no longer vanishes (migration 0080 repairs the stranded rows)",
    ticket: "2026-08-13 operator report in session (no ticket): 'those at final gate don't show up in the review tab'",
    status: "shipped_pending_verification",
    summary:
      "Grounded live: a gate nobody decides within the pipeline's 7-day window parks the production on_hold, and the cockpit Review page + list_gates listed only PENDING gates — so a video left the review queue at exactly the moment it had waited longest. Two real casualties found: the rendered 'Krypton' final cut (on_hold since 2026-07-26, invisible 18 days) and the published 'Jude' video, which a STALE pipeline run's timeout write clobbered from published back to on_hold a week after it went live (statusMismatch:true). Both also misclassified as haltKind 'precondition' (they predate migration 0073's halt_kind column), whose guidance is wrong for a timeout AND makes force_forward REFUSE the unblock. Shipped: (1) the Review page gains a 'Timed out — still waiting on you' section (on_hold + gate_timeout, with a failureReason fallback for legacy rows) and splits the old catch-all 'Final cuts' section — voiceover_recording gates (13 pending live!) were rendering AS final cuts, burying any real one; they now have their own 'Recording booth' section and finals are thumbnail_review only. (2) list_gates returns {gates, timedOutReviews, complianceBlocked} (SHAPE CHANGE from a bare array — documented in both guide mirrors). Operator follow-ups in the same session: sections now run in PIPELINE order, and productions clipped by a compliance flag (factuality gate / variation check / review board — e.g. the live 'review board: alignment' hold on The Lost Books) get their own 'Compliance flags — your judgement needed' Review-page section + the complianceBlocked list, with the same legacy-prose fallback (isComplianceBlock) so pre-0073 rows classify correctly and force_forward can waive them. (3) migration 0080 restores 'published' on timed-out rows that actually have a live publication, and backfills halt_kind='gate_timeout' on the rest, which also re-enables force_forward and the #94 P3 decide-refire for them. (4) the worker's five gate-timeout writes now only park a production that still sits at that gate's own review status — a stale run can no longer clobber a production that already moved on. (5) productionBlock classifies legacy timed-out rows as gate_timeout via isGateTimeout (pure, unit-tested).",
    nextStep:
      "Operator (after the Render deploy — migration 0080 on the worker preDeploy — and a connector reconnect for the list_gates shape): (1) open the Review page — 'Krypton: The Element Behind the Superhero Name' (01KXWM2W1ZFP2KC5ZRCSKFYQA1) should appear under 'Timed out — still waiting on you', the 13 recording-booth items should sit under 'Recording booth' (not 'Final cuts'), and the review-board hold on The Lost Books (01KZHTZQQ2JMQH8YDQD2E77B0C) should appear under 'Compliance flags' with its reason. Decide Krypton from its production page (or force_forward, which no longer refuses). (2) The Jude video (01KY6DN3EYT3SW9JNY0297GJDE) should read status 'published' again, matching its live upload. (3) list_gates over MCP should return {gates, timedOutReviews, complianceBlocked} with Krypton and the review-board hold listed until decided.",
  },
  {
    key: "bed-rotation-stamps",
    title: "#119 music-bed rotation was inert — every selection path now stamps lastUsedAt/usedCount; backfill migration 0078",
    ticket: "01KZPST41J5XY2EKTPMYHQM06B (#119)",
    status: "shipped_pending_verification",
    summary:
      "#119: get_music documented a least-recently-used rotation whose sort key was never written — only the pipeline's automatic pick stamped lastUsedAt, while every operator/agent selection path (set_production_music useBedStorageKey/useAudioAssetId/selectCandidateId, the cockpit music panel) did not, so the key stayed null on every track and the same track landed on three consecutive productions (which also concentrated the Convergence Content ID claim onto three videos). FIXED: a single stampBedTrackUsed helper advances the cursor (lastUsedAt + a new usedCount) from EVERY path that makes a track the selected bed; migration 0078 adds used_count and BACKFILLS both fields from real selection history (selected production_music rows joined to the production's channel, matched by storage key); ordering is a pure, unit-tested comparator (never-used first, then least-recently-used, then usedCount, then insertion order, then id — fully deterministic on a fresh bed); get_music reports lastUsedAt + usedCount per track and returns bed[] in rotation order (bed[0] = the render's next pick). Also fixed en route: useBedTrackForProductionAction's bed lookup was unscoped by channel. The ticket's mood-mismatch observation (bed tracks vs musicMood) is recorded in BACKLOG as a direction, not silently dropped.",
    nextStep:
      "Operator (after the Render deploy — migration 0078 applies on the worker preDeploy — and a connector reconnect for the new get_music fields): get_music on any Lost Books production — 'Unraveling' (published 2026-08-09) and the Convergence×3 history should now show non-null lastUsedAt and usedCount>0 from the backfill, and bed[] should order Convergence LAST. Then let the next production reach its track selection and confirm get_music shows the used track stamped and moved to the back. The acceptance's five-consecutive-productions distribution check is the live verification; the ordering logic itself closes on the unit tests.",
  },
  {
    key: "music-credit-verbatim-and-content-id",
    title: "#110 round 3 — verbatim/required credit formats, Content ID fields + attach-time warning, durationSec probe + backfill",
    ticket: "01KZJBQT4R6HNE70JCVTMQFA78 (#110 follow-ups)",
    status: "shipped_pending_verification",
    summary:
      "Three appended #110 findings. (1) MALFORMED CREDIT (escalated to blocking by the Sentric claim — the credit is what a rights administrator reads when deciding a release): musicCreditLines rebuilt the credit AROUND an audio-library track's stored attribution, which is already the complete T.A.S.L. line — title and licence printed twice with a stray `).,`. A self-contained line is now emitted VERBATIM (unit test asserts the licence appears exactly once and `).,` never does); legacy creator-(page) rows still compose. (2) CONTENT ID: audio_assets gains requiredCreditFormat (rights holder's wording, emitted verbatim in preference to the generated line), claimReleaseUrl, contentIdRegistered (migration 0079); publish + the post-publish metadata editor resolve the credit through the LIVE asset record via one shared selectedMusicCreditRow (so patching the asset fixes future descriptions without touching productions); set_music_bed/set_production_music return an 'expect an automatic claim' note when the catalogue is registered; the /audio page edits all three fields. (3) DURATION: durationSec is probed from the container header (mp3 Xing/CBR, wav, flac, m4a — honest null otherwise) on BOTH ingest paths (register_audio_asset streams past a tee; the /audio upload probes the assembled buffer), rows ingested pre-probe are lazily backfilled from the stored file on the next list/get/page read (persisted, once per row), the register/upload responses return it, and set_music_bed warns when a track is shorter than the channel's targetLengthSec.",
    nextStep:
      "Operator (after the Render deploy — migration 0079 on the worker preDeploy — and a connector reconnect for the new fields/notes): (1) list_audio_assets — the eight Scott Buckley rows should come back with non-null durationSec (backfilled on that first read), and minDurationSec:150 should return them. (2) patch_audio_asset on Convergence 01KZJRQQV7313V6Q9XCP64YSQR: requiredCreditFormat \"'Convergence' by Scott Buckley – released under CC-BY 4.0. www.scottbuckley.com.au\", claimReleaseUrl (his claims page), contentIdRegistered:true — then re-attach it anywhere and confirm the attach note appears. (3) On the next published library-track video, the description's Music bullet should match the required format character for character (set_publication_metadata description push re-assembles credits the same way for already-published videos). The formatting itself closes on the unit tests.",
  },
  {
    key: "rate-based-ken-burns-bundle-gate",
    title: "#114 rate-based Ken Burns — renders HOLD until the Remotion Lambda site bundle redeploys",
    ticket: "01KZK4NNYCSXQ919ZAJNGFAV0J (#114)",
    status: "shipped_pending_verification",
    summary:
      "#114 shipped (2026-08-09): stillMotionRatePctPerSec (0-3 %/sec — each shot's Ken-Burns travel = rate × ITS OWN hold length, capped at a 0.6 total delta, so a 28s hold visibly moves; wins over stillMotionAmount when set), stillMotion 'alternate' (push/pull flips by shot parity — free per-shot variety), stillMotionAmount cap raised 0.15 → 0.25, and a write-time warnings[] advisory on set_channel_config + set_production_profile when the effective amount÷hold rate lands below ~1%/sec (the ticket's 0.63%/sec 'the zoom didn't work'). The beat props now carry a PER-BEAT stillMotion the composition prefers over the global axis, which changes the rendered output — so COMPOSITION_BUNDLE_MIN_DATE was bumped to 2026-08-09 and the #78 preflight guard will REFUSE to render (haltKind 'precondition', BEFORE any voice/image spend) until the Lambda site bundle catches up. That refusal is the guard working, not a regression.",
    nextStep:
      "Operator: the Lambda site bundle must redeploy before any render succeeds — CI deploy-lambda-site runs on push to packages/video, and the worker preDeploy also redeploys it; if a render halts on the stale-bundle precondition after the Render deploy, run `pnpm lambda:deploy` manually. Then on a long-hold channel (e.g. minSecondsPerShot ~20-28): set_channel_config productionProfile {stillMotionRatePctPerSec: 1.2, stillMotion: 'alternate'} and render one video — long holds should visibly travel and alternate push/pull per shot. Writing a profile with only the old fixed amount over a long floor should return the warnings[] advisory (that part is unit-tested and closes on the tests).",
  },
  {
    key: "presenter-beat-slots",
    title: "#112 request 3 — presenter beat slots + footage-recording gate (deferred pending the operator's channel decision)",
    ticket: "01KZJXR25AD162RJH6DSPQH18V",
    status: "deferred",
    summary:
      "#112 shipped its ingest slice (2026-08-09): set_production_shot_video + the cockpit per-shot Footage upload attach operator-recorded video to any EXISTING shot (worker-trimmed to the shot window; assetType operator_clip on the disclosure surface; image regens no longer delete an operator clip; render always keeps operator footage). DELIBERATELY NOT BUILT YET: request 3 — beats[].presenter declarations at author_script, the shot planner RESERVING those slots instead of generating them, and a hold at a recording gate listing awaiting shots (the voiceover_recording shape). That is the piece that avoids paying for generated shots the operator intends to replace, but it changes pipeline gating and the operator filed the ticket as INFO while still EVALUATING the presenter-format channel — per the house rule, live-behaviour changes ship when the operator calls the start. The current path costs ~11 generated stills per long-form that get covered by footage anyway (posters), which is cents. Also noted in the ticket, filed separately when the operator proceeds: the MAP/DIAGRAM/GRAPHIC capability gap (31% of screen time in the reference format).",
    nextStep:
      "Operator: when the presenter-format channel gets the green light, say so — the ticket stays open for request 3, and the planner/gate design is specced in the resolution comment. Until then: attach footage post-visuals with set_production_shot_video (or the visuals grid's Footage button) and Retry from render.",
  },
  {
    key: "dna-consistency-and-slate-exceptions",
    title: "#109 write-time DNA consistency warnings + one-off review_slate block acceptances",
    ticket: "01KZJADTF66F8X602MK9MRGTZJ",
    status: "shipped_pending_verification",
    summary:
      "#109, all three asks. (1) set_channel_config now warns (advisory, stored-as-written) when a forbiddenTopics entry carries an UNBOUNDED temporal qualifier ('recent-era' read a 1988 incident as recent) — deterministic, unit-tested, also surfaced on get_channel_config.consistencyWarnings. (2) Writing titleTemplates or forbiddenTopics runs a one-shot semantic evaluator (config_consistency agent, cheap tier — the first LLM call set_channel_config makes, try/caught so a provider outage never fails a config write) that flags a declared title family whose FAITHFUL instances a forbidden topic prohibits; the verdict persists in channel_dna.consistency_findings (migration 0075) and replays on every get_channel_config read without re-billing. (3) accept_slate_finding(channelId, title, rule, reason): records the operator's one-off acceptance of a review_slate blocking finding as a channel_decisions row (new kind 'slate_exception', migration 0075) — the audit distinction the ticket asked for between judgement exercised and a constraint deleted; review_slate then reports the matching block under acceptedFindings[] (visible, reason attached) instead of blocking the verdict. Matching is (rule + exact title, normalised). The standing rule keeps blocking everything else. NOTE the ledger row reaches generation prompts via channelStateSummary's last-10-decisions window — an accepted exception is a real editorial decision, so that is intended, but worth knowing.",
    nextStep:
      "Operator (after Render deploy — migration 0075 applies on the worker preDeploy — and a connector RECONNECT for accept_slate_finding + the new response fields): (1) get_channel_config on Wings & Stories 01KZJ8JD60015B3NTEEGAPQH50 — consistencyWarnings should flag 'recent-era' in forbiddenTopics[0] deterministically. (2) Re-write dna.titleTemplates (send the same array back) — the semantic check should then flag the aircraft-that-did vs machine-profile contradiction in the response warnings and on subsequent reads. (3) On the next blocked slate, accept_slate_finding with a reason, re-run review_slate, and confirm the block moved to acceptedFindings[] with the reason attached. The deterministic parts (temporal regex, stored-verdict replay, exception partitioning) are unit-tested and close on the tests; the semantic evaluator's judgement quality is the live check.",
  },
  {
    key: "free-music-import-headers",
    title: "#110 free-music path, round 2 — streaming imports (no length limit), music-first search, and the platform audio library",
    ticket: "01KZJBQT4R6HNE70JCVTMQFA78",
    status: "shipped_pending_verification",
    summary:
      "#110 round 1 (browser-shaped headers + real error reasons + search-time probe) was LIVE-CONFIRMED by the operator: a 29s track imported and the new error text exposed the next defect in two calls. Round 2 ships the operator's follow-ups. (1) TIMEOUT/STREAMING: the 30s whole-download budget killed every track of usable length (349s/580s mp3s — the tool worked only for files too short to score a video); importTrack now STREAMS a known-length body straight to the store (peak memory = a chunk, same lesson as the chunked YouTube upload) under a 120s budget, and the probe honestly reports REACHABILITY (renamed importCheck.reachable) + the full sizeBytes from the 206's content-range instead of implying a download guarantee. (2) SEARCH: every result across five queries was a freesound one-shot — no source was ever restricted, but with no category filter the sound-effects library dominated relevance; the query is now category=music biased (surfaces Jamendo's actual music catalogue) with a minDurationSec filter (default 150s, Openverse length buckets + client-side guarantee), degrading to the unfiltered query rather than empty results, and the CC0/PDM/BY/BY-SA licence filter (never NC/ND) is asserted on every branch by unit test. (3) THE LIBRARY (operator feature request, supersedes the narrow asks): migration 0076 adds platform-scoped audio_assets with full T.A.S.L. provenance; register_audio_asset (URL fetch + honest licence-page enrichment) / list / get / patch tools; cockpit /audio upload page; set_music_bed(addLibraryAssetId) + set_production_music(useAudioAssetId) with commercialUse ENFORCED (NC/ND/unknown refused outright — monetised = commercial use); and the discovered-and-fixed silent breach: MUSIC ATTRIBUTION NEVER REACHED THE PUBLISHED DESCRIPTION (only image credits did, while the bed schema claimed otherwise) — production_music now carries attribution/license through every copy path and publish appends a 'Music:' credit block. 22 provider + 14 core licence tests.",
    nextStep:
      "Operator (after Render deploy — migration 0076 on the worker preDeploy — and a connector RECONNECT for the 4 new tools + renamed importCheck.reachable + new args): (1) the ticket's own acceptance: set_music_bed(addOpenverseTrack) on bb9c593e-… (349s) and ef6aadad-… (580s 'Thriller Ambient') — both should now import within the 120s budget. (2) search_free_music('dark ambient') — the majority of results should be >150s, ideally with Jamendo sources alongside freesound. (3) Upload one of the manually-downloaded tracks on /audio (or register_audio_asset with its URL + source page), set its licence, pull it into a bed with addLibraryAssetId, and confirm an NC-licensed test asset is REFUSED. (4) On the next published video with a CC BY bed track, check the description for the 'Music:' block.",
  },
  {
    key: "operator-voiceover-assembly",
    title: "Operator narration assembled one take per beat on repeat (#103)",
    ticket: "01KZCN30N66C2HK9FC9HFA8BQ7",
    status: "shipped_pending_verification",
    summary:
      "#103: the first production narrated by the operator (Dog-Eared, 122 recorded segments) assembled into ~9 minutes of one segment repeating instead of the 122 takes in order. CAUSE, confirmed in code: assembleOperatorVoiceover named its working files by BEAT INDEX (raw-<beat>, norm-<beat>.wav, and the TTS-fill storage key vo-tts-<beat>). That was unique while a piece WAS a beat, and became silently wrong when #101 cut each beat into sentence-sized segments — every segment of a beat wrote the same temp file, the last one won, and the ffmpeg concat list referenced that single file once per segment. Nothing threw; the run reported all 122 pieces. It also explains the duration gap (~540s observed vs ~953s expected): each beat contributed its LAST segment's duration once per segment, and a greedy-packed final segment is the short one. FIXED: pieces are named by their ordinal in the assembly (pieceSlug), which is unique by construction, and the plan is asserted 1:1 before any audio is fetched so a future regression FAILS instead of shipping repeated audio. THE 122 TAKES WERE NEVER AT RISK — /api/voiceover-take stores each under vo-take-<encoded segment idx> and the assets row is unique on (productionId, kind, idx), so no re-recording is needed. The ticket's third hypothesis (a missing state write behind assembled:false) was WRONG: the cockpit only renders the voiceover player when the asset row exists, so the row existed when the operator heard the track; halting with discard:['voiceover'] deletes the row and keeps the storage bytes, which is exactly the assembled:false-with-audible-audio shape. get_production().voiceover now reports assembledAt/assembledPieces/assembledDurationSec and an assemblyWarning naming that orphan case, and productionBlock now covers 'halted' (it wrote no failureReason by design, so a deliberately-stopped run read blocked:null — the healthy shape).",
    nextStep:
      "Operator (after the Render deploy + a connector reconnect): on production 01KZAYP75EPPCWJQRWMSDRH9MT, continue_production (or reopen_stage('voiceover')) to re-assemble, then LISTEN — the track should run the 122 takes in order at roughly the sum of their durations, not one segment on repeat. get_production().voiceover should then read assembled:true with assembledPieces == segmentCount (122) and no assemblyWarning. NOTE reopen_stage('voiceover') marks the VISUALS stale (shot boundaries are cut from the voiceover) — on this production the run halted before visuals, so nothing is lost. The naming invariant and the halted-blocked mapping are unit-tested; the audible result is the one part the sandbox cannot check (no prod DB, no stored takes here).",
  },
  {
    key: "chunked-youtube-upload",
    title: "Long-form uploads no longer OOM the worker (chunked Content-Range PUTs)",
    ticket: "2026-08-05 memory review (no ticket — found live)",
    status: "shipped_pending_verification",
    summary:
      "Pentimento's 'When You Stop Being the Good One… | Carl Jung' (42 min, 178 shots) failed to reach YouTube across FIVE attempts while the render succeeded every time. Cause, confirmed from three independent signals: the Inngest run shows check-quota-0 COMPLETED (so it was never quota) and then the upload step running 4m47s before 'server returned HTTP 502 before the SDK responded' with no step output; Render's metrics show the worker climbing from its ~350MB baseline to a sustained ~1.7GB of the 2GB cap for the length of that step; and Render emailed a memory-limit breach with four instance restarts between 03:32 and 03:37 UTC. The single-shot upload passed the whole store stream to fetch as a request body — which reads as streaming, sets duplex:'half' correctly, and still holds the file. Replaced with chunked resumable upload: 8MiB Content-Range PUTs (a 256KiB multiple, as YouTube requires for every chunk but the last), so peak upload memory is one chunk regardless of file size, and a failed chunk no longer discards the whole upload. The truncation guard (sentBytes vs contentLength) is preserved and a new guard fails loudly if YouTube accepts every byte but returns no video resource.",
    nextStep:
      "Operator: publish a LONG (30min+) video and confirm it reaches YouTube, and that the worker's memory graph stays near its ~350MB baseline through the upload instead of climbing to ~1.7GB. Short videos already published fine and will NOT exercise the bug. The stuck episode (production 01KYRBCPPPQC683NRYTQMD9YMZ) is the natural test case; it can also be published by hand from /api/media/productions/01KYRBCPPPQC683NRYTQMD9YMZ/final.mp4?download=1 plus sync_publication_from_youtube. Unit tests cover the chunk-size discipline and byte-order preservation; the memory profile itself is only observable live.",
  },
  {
    key: "carried-forward-from-closed-tickets",
    title: "Known-unfixed work carried forward from tickets closed in the 2026-08-04 board sweep",
    ticket: "#96 (clip ledger) / #100 (thumbnail overlay text)",
    status: "deferred",
    summary:
      "The 2026-08-04 sweep closed the ticket board under the new close-when-shipped policy. Two pieces of work were knowingly NOT fixed and are recorded here so closing the parent ticket did not bury them. (1) #96: assetCounts reported generatedClips 8 vs clipsBilledToVideoEngine 9 on production 01KZ4VQ949DTDYY7HPR49WDH14 — a clip billed to the video engine that produced no usable output, or a phantom/stale clip row. The #67 ledger cross-check surfaced it correctly; diagnosing it needs the cost-ledger rows, which aren't reachable from the sandbox. (2) #100: the channel thumbnail template derives the overlay headline from the FIRST TWO WORDS OF THE VIDEO TITLE, which produced 'TERTULLIAN EXPLAINED' on an episode whose searchable, clickable term is ENOCH — that term appears nowhere on the thumbnail. This recurs on every episode of the channel. The template should take an explicit hook string, or omit text entirely and let an authored thumbnailPrompt supply it, rather than slicing the title. 2026-08-09 board sweep: #77, #78 and #99 are now CLOSED WITH FIXES — #77: set_publication_metadata pushes title/description/tags to a published/scheduled video via videos.update (read-merge on the snippet so omitted fields keep their live values; a pushed description is re-wrapped with the AI disclosure + licence credits via the shared core/publish-credits helpers; sync_publication_from_youtube returns liveTitle + a drift note). #78: force_forward is REFUSED on a precondition on_hold (pure forceForwardRefusal in halt.ts, enforced in the action + named in the cockpit panel, which is now haltKind-aware); the stale-bundle check ALSO runs at render-preflight before any voice/image spend; the halt-taxonomy prose no longer files the stale bundle under external_retryable; retry buttons state their re-billing consequence. #99: closed on the shipped identity/roster/alert work (rotation stays the operator's decision), plus a post-ship gap fixed — tools/call receipts now borrow the clientName/clientVersion from the most recent same-origin handshake, so billable rows group with their client instead of collapsing into anonymous per-IP buckets.",
    nextStep:
      "Operator: file the clip-ledger discrepancy and the thumbnail-overlay-text template as their own tickets when either next bites — both are described in full above, so a fresh ticket only needs the current evidence. #77/#78/#99 are closed; reopen from chat if a fix doesn't hold. #77 live check: retitle 01KY6DN3EYT3SW9JNY0297GJDE via set_publication_metadata and confirm on YouTube. #78 live check: the Pentimento production's panel should show the guard callout instead of force-forward.",
  },
  {
    key: "production-flow-redesign-p1-p6",
    title: "Production flow redesign P1-P6 — halt taxonomy, health object, gate-timeout dead end, authoring intentions, in-place recovery, early compliance checks",
    ticket: "flow assessment (2026-08-04), follow-on from #94/#95/#96/#97/#98/#99",
    status: "shipped_pending_verification",
    summary:
      "SHIPPED (2026-08-04). A trace of every failure path in production-pipeline.ts found 20 pre-publish exits, 19 of which wrote status 'on_hold' distinguished only by free text — so a reviewer rejecting the visuals and YouTube exhausting its upload quota produced the same row, and the operator had to read prose to choose between four recovery verbs (one of which re-bills every image). #94/#97/#98 are all variants of that. Six changes. P1 (migration 0073): haltKind enum on productions — human_decision | gate_timeout | compliance_block | external_retryable | precondition — supplied by all 19 on_hold exits plus the one 'rejected' exit; core/halt.ts holds the policy table (summary, recommendedAction, canAutoRetry) so the cockpit button, the MCP response and any future retry loop cannot disagree. P5: get_production returns `blocked` — null when healthy, else the full health object; canAutoRetry is true ONLY for external_retryable. P3: found and fixed the mechanism behind #94 that could not be explained at the time — a gate decision is only heard by a LIVE run waiting on production/gate.decided, so deciding a gate that had TIMED OUT marked it 'decided' (hiding it from list_gates, which returns only pending rows) while the production sat untouched forever. decideGateAction now detects that case (status on_hold + haltKind gate_timeout, which provably means no run is listening) and re-fires the pipeline. P6 (migration 0074): scriptAuthored/promptsAuthored/motionAuthored replace the single externalScript flag that silently governed all three behaviours; null inherits the legacy flag so every existing row behaves identically, and resume/corrected-copy now carry them as a struct that cannot be half-lost (#94's failure mode). P4: resume_production gains inPlace:true — recovers the SAME production instead of minting the same-idea sibling whose lineage caused #94/#96/#97. P2: productionProfile.earlyComplianceChecks (OPT-IN, DEFAULT OFF) runs variation/anti-clone/review-board BEFORE the visuals gate rather than after, so a block lands on work nobody has reviewed yet; the checks were hoisted into one closure invoked at exactly one of two positions, with identical step ids either way so Inngest memoization is unaffected. 13 new unit tests; core/db/agents/worker/cockpit typecheck + cockpit build + 487 core tests. Both guide mirrors + HANDOFF + BACKLOG updated.",
    nextStep:
      "Operator (connector RECONNECT for the new fields; migrations 0073+0074 apply on the worker preDeploy): (1) get_production on the stranded 01KZ4VQ949DTDYY7HPR49WDH14 — `blocked` should now name the class and the verb instead of a bare status. Older rows carry no haltKind and degrade to the conservative 'precondition' policy, which is expected, not a bug. (2) P2 is DEFAULT OFF and is the one change that alters pipeline ordering — do NOT enable earlyComplianceChecks unattended; turn it on for one channel with you present and watch a full run. (3) P4's inPlace:true and P6's intent fields take effect on the next resume; existing rows are unaffected. (4) P3 is only observable on a gate that has ALREADY timed out — deciding one should now return resumed:true and move the production. The taxonomy, health object and intent resolution are unit-tested (halt.test.ts), so P1/P5/P6 close on the tests; P2/P3/P4 need a live run.",
  },
  {
    key: "stage-re-entry-engine",
    title: "Stage re-entry engine — Hold / Continue / Reopen in place, with a tested invalidation cascade (operator design session)",
    ticket: "n/a (2026-08-04 operator design session)",
    status: "shipped_pending_verification",
    summary:
      "SHIPPED (2026-08-04). The engine behind the standing complaint 'every time we hit an issue, it pushes me way back to the start' — which was structurally true: force_forward and retry_production both re-fire production/greenlit and lean on skip-if-present, so every recovery LOOKED like a restart, and resume_production went further and minted a whole new production row. New pure core module production-stages.ts defines the stage machine (script/voiceover/visuals/music/render/thumbnail/publish) and, critically, the INVALIDATION CASCADE as a tested pure function: script -> voiceover, visuals, render, thumbnail; voiceover -> visuals, render (shot boundaries are cut from the voiceover's word timestamps, so new audio re-cuts the shot plan and the existing stills no longer match their lines — the script survives, the shots cannot); visuals -> render; music -> render. Deliberately NOT cascaded: re-cutting the visuals keeps the operator's chosen music bed and the thumbnail. Three verbs, all IN PLACE on one row: halt_production = HOLD (in-flight generation completes and is kept, nothing new dispatched — matches the operator's 'anything sent to request generation continues, but no new fires go off'); continue_production = CONTINUE (resumes from where it stopped, deletes nothing, re-bills nothing, and lands on the work that EXISTS rather than upstream of it — #98's lesson); reopen_stage(stage, mode) = go back, where mode 'reopen' KEEPS that stage's own output so shots can be refined individually and mode 'clean' rebuilds it (the operator's 'clean visuals, remove visuals that exist' vs 'refine prompts for shots' — two different operations). Downstream work is marked STALE and left on disk; it is destroyed only when the reopened stage actually produces new output (a sweep step at the top of the pipeline), so cancel_reopen restores the production untouched until then — reopening is frequently diagnostic and a diagnostic action must not be destructive. resume_production is now documented LEGACY: minting a sibling production from one idea is the lineage behind #94, #96 and #97; the only case that still needs a new row is correcting an already-published video, since YouTube cannot replace a live file. Migration 0072 (reopened_stage, reopen_mode, reopened_at). 29 new unit tests; core/db/agents/worker/cockpit typecheck + cockpit build + 503 core tests. Both guide mirrors + HANDOFF + BACKLOG updated.",
    nextStep:
      "Operator (connector RECONNECT for the three new tools; migration 0072 applies on the worker preDeploy): (1) reopen_stage(productionId, 'voiceover', {confirm:false}) on a built production — the PREVIEW should name the images/clips/render it would discard, say the shots go because timings are cut from the voiceover's word timestamps, and list the script/music/thumbnail as kept. Nothing should change. (2) Apply it, confirm get_production returns `reopen` with staleStages, then cancel_reopen and confirm the production is exactly as it was with nothing deleted. (3) halt_production then continue_production on a production with images — confirm it resumes at producing_assets (never greenlit), no new production id, and get_production_costs is unchanged. The cascade and warning rules are unit-tested (production-stages.test.ts, 29 cases) so the engine closes on the tests; the live re-run is the operator's check. NOT YET BUILT (second pass, by agreement): the separate thumbnail and music GATES — the stage machine already models both stages, so gating them is additive.",

  },
  {
    key: "production-flow-recovery-batch-95-99",
    title: "#95/#96/#97/#98/#99 — recovery-path batch: style double-prefix, resume carrying stale profile + sourced stills, variation self-match, force_forward regressing to greenlit, MCP call attribution",
    ticket: "#95/#96/#97/#98/#99 (2026-08-04)",
    status: "shipped_pending_verification",
    summary:
      "SHIPPED (2026-08-04). Five tickets, four of them on the RECOVERY paths (halt/resume/force_forward) rather than the happy path. #95 (warn): applyHouseImageStyle prefixed 'Style: ' onto a register that already began with 'Style:' — a distilled style's promptSuffix is authored that way by the distiller — producing 'Style: Style:' on every authored shot of every channel with a distilled style. Fixed with a case/whitespace-tolerant check; 3 unit tests. #96 (error): archivalStrength 'off' + visualMode 'ai_images' were set and stored, yet 23 of 102 shots were real photography. archivalImagePolicy is CORRECT — those shots were never generated by that run at all (styleSource/engineRequested/engineServed all null, the tell): they were COPIED by resume_production from a grandparent created before the setting changed. Two fixes: copyProductionMedia no longer carries SOURCED stills into a production whose resolved profile forbids sourcing (those shots regenerate under the current policy), and the per-video profile carried by #94 is now MERGED over the channel's CURRENT config instead of freezing the ancestor's snapshot. #97 (error): the variation check compared a production against SIBLING productions of the same idea — every recovery path mints siblings that reuse the parent's substanceFingerprint verbatim, so jaccard=1.000 (a perfect set match) stranded $6.95 of human-approved work in on_hold immediately after the operator approved the visuals gate. The corpus is now published/scheduled rows of OTHER ideas only (the old predicate excluded rejected/halted/failed/on_hold but NOT in-flight gate rows, which is exactly what tripped it), and failureReason now NAMES the counterparty production + title. #98 (error): force_forward wrote status 'greenlit' — upstream of scripting, producing_assets, assembling and a human-APPROVED visuals gate — so a fully-built production read as 'kicked back to the script gate'. It now presents the status matching the work that exists (assembling with a render, producing_assets with images); the re-fire event is unchanged because the pipeline is idempotent. Its sibling defect: stuckReviewStates (#94) watched only *_review, so a production stranded at greenlit was invisible to the very detector built to catch stranded productions — replaced by stuckProductions(), which watches every non-terminal status and still exempts a review status with a LIVE gate (that is a legitimate wait on the operator). #99 (error, SECURITY): mcpCalls receipts carried no client identity, source or target, so three unaccounted BILLABLE image generations were indistinguishable from a leaked connector URL being used by a third party. Added client attribution (migration 0071): a stable clientId derived from the initialize handshake's clientInfo plus a SALTED HASH of the source address (the address is never stored), clientName/clientVersion/ipHash, and targetChannelId/targetProductionId per call; get_diagnostics returns an mcpClients roster; a billable/publishing call from a never-seen client raises a CRITICAL alert naming the tool, client and target, and pointing at MCP_BEARER_TOKEN rotation on /account. 18 new unit tests; core/db/agents/worker/cockpit typecheck + cockpit build + 474 core tests. Both guide mirrors + HANDOFF + BACKLOG updated.",
    nextStep:
      "Operator (connector RECONNECT for the new return fields; migration 0071 applies on the worker preDeploy): (#99 FIRST, it is the security one) get_diagnostics().mcpClients — the roster starts empty and fills from the next call, so it will NOT retroactively identify the 2026-08-03 23:13-23:20 calls; those remain unattributable. Decide on that basis whether to rotate MCP_BEARER_TOKEN on /account now (recommended — it invalidates the old URL immediately and costs one reconnect). From then on an unrecognised client running a billable tool raises a critical alert. (#98) get_diagnostics().stuckReviewStates should now list production 01KZ4VQ949DTDYY7HPR49WDH14 at greenlit with its age — that row still needs one manual push (force_forward again, now that it will land at producing_assets, or retry_production('render') which reuses the existing 102 images and only re-renders). (#97) the block that stranded it will not recur: re-run and confirm no on_hold with jaccard=1.000; a genuine block now names the production it matched. (#96) resume an authored production on a channel with archivalStrength 'off' and confirm get_production_shots reports 0 sourced. (#95) renderedPrompt should contain exactly one 'Style:'. All five have unit tests for their deterministic parts, so the logic closes on the tests; the live effects are the operator's check.",
  },
  {
    key: "authored-prompt-style-register-reopen",
    title: "#93 REOPENED — an active distilled style suppressed the register entirely; authored shots rendered styleless on seedream",
    ticket: "#93 (reopened 2026-08-03), ticket 01KZ070KJW60WRJSVCJ778F6D4",
    status: "shipped_pending_verification",
    summary:
      "SHIPPED (2026-08-03, second fix). The operator verified the first two passes on a LIVE render (production 01KZ3T4RJARG54GSSKEEF33Q6R, worker c729bde, externalScript true, imageStyle complete at ~980 chars, fresh seedream images) and shots were STILL photoreal — $0.4728 wasted. Root cause: both earlier passes gated the register on 'no distilled Style-tab style is active', on the stated belief that an active distilled style rides authored prompts as reference-image conditioning instead. That belief was FALSE and is the whole bug. On this channel all three carriers missed at once: (a) dna.imageStyle was suppressed by the !ctx.style carve-out because a distilled style existed; (b) the distilled style's TEXT (styleBlock) is woven in by buildImagePrompts, which authored prompts skip by definition; (c) the distilled style's reference-image conditioning fires ONLY on nano-banana (deliberately dropped on qwen/seedream, where an edit model mangles a style ref) and only within its conditioning scope — and this channel renders on seedream. So the prompt reached the model with no register at all. Fix: new pure resolveShotStyleRegister() in core picks the register with NO carve-out — the distilled promptSuffix when a style is active (it is what the builder would have woven in), else dna.imageStyle, else none — and a builder-skipped prompt ALWAYS gets it as text at both the pipeline and redraw choke points; reference conditioning, when it also fires, now stacks on top exactly as it does for builder-written prompts. Observability (the operator's ask, since verifying previously cost a render + a human eyeball): get_production_shots[] now returns renderedPrompt (the EXACT string sent to the image engine, register included), styleSource (distilled_style|channel_image_style|none), authoredPrompt and styleConditioned; get_channel_config now returns activeStyle (the distilled style or null, with conditioningScope/refCount) and shotStyleRegister {source, register} — whether a Style-tab style is active was previously invisible over MCP, which is why the operator could not rule it in or out. 4 new unit tests pinning the regression. Both guide mirrors CORRECTED — they documented the false precedence claim.",
    nextStep:
      "Operator (connector reconnect needed for the new return fields): (1) FREE CHECK FIRST — get_channel_config(01KY291HX68DFJYH6QBCG9Y3FC) and read shotStyleRegister: source must be distilled_style or channel_image_style, never 'none', and activeStyle now tells you whether a Style-tab style is active (the thing that was invisible). (2) author_script a SHORT test production and read get_production_shots[].renderedPrompt — it must end with the register, and styleSource must name it. That is a one-image check, not 11. (3) Only then judge the render. The register-selection rules are unit-tested (visual-style.test.ts, 'resolveShotStyleRegister — the live-render regression'), so the logic closes on the tests; the rendered look is the live check. NOTE: the previous two resolutions on this ticket claimed the pipeline path worked; it did not on a channel with an active distilled style. Treat renderedPrompt/styleSource as the evidence from now on rather than any claim in a resolution comment.",
  },
  {
    key: "resume-carries-settings-and-orphaned-review-states",
    title: "#94 — resume_production dropped externalScript/productionProfile; a production parked in a review status with no gate was invisible",
    ticket: "#94 (2026-08-03), ticket 01KZ3PCTYAG91DJQM64E911G45",
    status: "shipped_pending_verification",
    summary:
      "SHIPPED (2026-08-03). Two defects on the resume path. (1) resumeProductionAction inserted the new production with only ideaId/channelId/status/substanceFingerprint — it dropped externalScript and productionProfile (the corrected-copy path has always carried them, with a comment saying exactly why). Losing externalScript un-authors an operator-authored production: the pipeline's skipScriptGate goes false so it re-presents the script gate, authoredPrompts goes false so the image-prompt BUILDER rewrites every authored imagePrompt instead of using it verbatim (which also takes #93's authored-prompt style path out of play), and authored motionPrompts stop being honoured — the operator's 126 authored prompts were silently discarded on resume. Losing productionProfile makes propose-profile-tweaks re-run and mint a FRESH profile_review gate on a video whose profile was already decided — how the reported production entered a review state at all. Fix: resume now carries externalScript, productionProfile, voiceSource/voiceVolume/musicVolume and personaId/personaVersion/styleId/styleVersion. (2) A production parked in a *_review status with NO pending gate row is invisible: list_gates returns only PENDING gates and get_diagnostics.blockedProductions only covered failed/on_hold, so the row sat unapprovable until the pipeline's gate timeout stranded it (the operator read it as 'voiceover is stuck'; voiceover had not been reached). Fix: new pure orphanedReviewStates() in core (gate-lifecycle.ts) — the mirror of the existing outlived-gate rule — surfaced as get_diagnostics.stuckReviewStates with the age and the force_forward unblock, ignoring the legitimate post-decision transition window. 7 new unit tests; core + cockpit typecheck, cockpit build, 452 core tests. Both guide mirrors + HANDOFF + BACKLOG updated.",
    nextStep:
      "Operator: (1) get_diagnostics() — stuckReviewStates should now name production 01KZ3P2512Y5M3DKCPB81XQVPN (profile_review, no gate) with its age; force_forward it (or retry_production) to unpark it. This is DETECTION, not a repair of the existing row — the parked production still needs that one manual push. (2) The resume carry-over only applies to resumes performed AFTER this deploy: halt + resume_production an authored production and confirm get_production shows externalScript:true, that no script gate and no profile_review gate appear, and that get_production_shots still shows YOUR imagePrompt text rather than builder-rewritten prose. The detector's rules are unit-tested (orphaned-review-states.test.ts) so #94's logic can close on the tests; the live re-run is the operator's check. NOTE: the exact mechanism that left the reported row gate-less could not be reproduced from the sandbox (no prod DB) — the detector covers the STATE regardless of cause, which is what the ticket asked for.",
  },
  {
    key: "authoring-visuals-fidelity-batch",
    title: "Authoring/visuals fidelity batch — #93 authored-prompt style, #91 thumbnail prompts, #92 sourced vision-verify, #90 character constraints, #89 config caps, #70 per-shot casting",
    ticket: "#93/#91/#92/#90/#89/#70 (2026-08-02)",
    status: "shipped_pending_verification",
    summary:
      "SHIPPED (2026-08-02), six operator tickets on authoring/visuals fidelity. #93 (error): an authored imagePrompt bypassed the builder LLM and so never got the channel house dna.imageStyle — a 'NOT photographic' channel rendered photoreal. Fix: the pipeline now appends dna.imageStyle to authored prompts as a render-register suffix at the single generateImage choke point (subject/composition stays verbatim), only when no distilled Style-tab style is active (that rides as reference-image conditioning and wins); author_script's resolvedProfile now echoes imageStyle. FOLLOW-UP (2026-08-03): the first pass covered the pipeline only — regenerateShotImage (the REDRAW path behind regenerate_shot and edit_shot_prompts(regenerate:true)) renders an operator-typed/stored prompt straight, with no builder, so the ticket's own stated remediation ('edit_shot_prompts with regenerate:true across all 118 shots') would have reproduced the styleless look exactly. The rule is now one tested pure helper in core (applyHouseImageStyle: appends 'Style: <register>', idempotent, no-ops on a blank style or a prompt that already carries it) used at BOTH choke points; the redraw path takes the distilled Style-tab promptSuffix when a style is active, else dna.imageStyle (unlike the pipeline it applies no style reference conditioning, so the suffix is the only register available). A re-derived prompt is left alone — the builder already wove the style in. author_script's shotPlan.notes now states, whenever authored prompts are present, that they are verbatim for subject/composition with the house register applied (or that the channel has NO imageStyle set) — the warning the ticket asked for. 6 new applyHouseImageStyle unit tests. #91 (error): auto thumbnail overlay text was the BACKLOG IDEA title truncated mid-phrase ('THE B47 AND') and a 9:16 'proven-winner' template (Vertical/mobile crop, cute-subject) was stamped on a 16:9 long-form frame. Fix: overlay text now comes from the AUTHORED publication title, takes the first natural clause, and never ends on a dangling connective (omits rather than emits a fragment); winner patterns are queried for the frame's own format and buildThumbnailPrompts skips any pattern whose composition contradicts the aspect. #92 (warn): regenerate_thumbnail's referenceEntity silently fell through from archival to generic stock and returned the wrong aircraft as a valid sourced candidate. Fix: every sourced candidate is vision-verified (scoreImageFit vs the subject, IMAGE_FIT_MIN) before it's offered; list_thumbnails returns sourceTier (archival|stock_fallback) + fitScore; a stock fall-through that can't be verified is dropped. #90 (warn): create_character's brief->description distiller dropped proportional constraints ('legs half his height', 'not dwarfish') → squat renders. Fix: new verbatim constraints passthrough on create/refine_character (stored + appended to the reference-sheet render untouched), the distiller prompt now preserves measurements/negations, and the response returns droppedConstraints[] when a brief measurement didn't survive (migration 0070_character_constraints.sql). #89 (warn): set_channel_config silently truncated titleTemplates[].pattern at 500 and imageStyle at 400. Fix: caps raised to 2000 and any remaining truncation returns a warnings[] entry naming field/limit/length; sibling create_channel + cockpit UI caps raised to match. #70 append (warn): characterId lived only on regenerate_shot. Fix: added characterId to edit_shot_prompts.shots[] (bulk per-shot casting; the whole worker path already consumed it) with up-front channel-scoped validation; guide corrected (casting is also per-shot, not only per-channel castMode). 17 new unit tests (thumbnail-prompts + character-constraints + applyHouseImageStyle); core/agents/worker/cockpit typecheck + cockpit build + 445 core tests pass. Both guide mirrors + HANDOFF + BACKLOG updated.",
    nextStep:
      "Operator (after a connector RECONNECT for the new params/return-fields, and after migration 0070 deploys via worker preDeploy): (#93) author_script a production whose beats carry full imagePrompts on a channel with dna.imageStyle 'illustrated, NOT photographic' — confirm resolvedProfile.imageStyle echoes it, that shotPlan.notes carries the authored-prompts line, and that the rendered shots are illustrated, not photoreal; then edit_shot_prompts one shot with a plain authored prompt and regenerate:true and confirm the REDRAW is illustrated too (that redraw path was still styleless until the 2026-08-03 follow-up). The append/idempotence/blank-style rules are unit-tested (applyHouseImageStyle), so #93's logic can close on the tests; only the rendered look needs the live check. (#91) at thumbnail_review read list_thumbnails().prompt — overlay text should be a complete phrase from the PUBLICATION title, and no candidate should carry 'Vertical/mobile crop' on a 16:9 channel. (#92) regenerate_thumbnail(referenceEntity:'B-47 Stratojet …') then list_thumbnails — sourced candidates should carry sourceTier + fitScore and no wrong-subject stock photo. (#90) create_character with a heavy build + a proportional brief and confirm droppedConstraints[] surfaces anything lost; pin it via the constraints field and confirm the sheet is correctly proportioned. (#89) set_channel_config a >2000-char pattern and confirm a warnings[] entry (no silent truncation). (#70) edit_shot_prompts(shots:[{shotIndex, characterId, imagePrompt}], regenerate:true) — confirm the cast lands on those shots. Deterministic parts (thumbnail overlay/aspect gating, constraint detection) are unit-tested, so #91/#90 can close on the tests; the live-render effects are the operator's check. No live YouTube API / prod render from the sandbox.",
  },
  {
    key: "shorts-derivation-subchannel-model",
    title: "Shorts derivation Phase 2 — subchannel model + shared publish-auth (youtubeAuthChannelId)",
    ticket: "n/a (operator design session, SHORTS-DERIVATION-SPEC §1/§7)",
    status: "shipped_pending_verification",
    summary:
      "Phase 2 SHIPPED (2026-08-01): the subchannel model that lets a derived Shorts channel publish to the PARENT's YouTube account (Mode 1, the default — Shorts are native to one channel) or its own (Mode 2, a separate Shorts channel). New column channels.youtubeAuthChannelId (migration 0069_subchannel_youtube_auth.sql) is the publish-AUTH pointer; new pure module packages/core/src/subchannel.ts resolves it: pickAuthChannelId (self when null/blank/self-referential — one hop, no cycles), resolveYoutubeAuthChannelId (DB wrapper), subchannelChannelFields + subchannelAuthChannelId + subchannelPublishTarget (build/read the fields for a subchannel row). loadChannelToken now FOLLOWS the pointer, so BOTH the publish path and the analytics path transparently use the parent's OAuth token for a Mode 1 subchannel. DEFAULT-OFF: youtubeAuthChannelId is null on every existing channel, pickAuthChannelId returns the channel's own id, so behavior is byte-for-byte unchanged until the operator creates a subchannel. OPERATOR-WIRED 2026-08-08 (#104): the plumbing had sat inert because nothing could write the auth pointer — create_channel wrote derivedFromChannelId but never youtubeAuthChannelId, so an MCP-created subchannel silently landed on Mode 2 with a token it doesn't have, and no tool returned either field. set_channel_config now takes derivedFromChannelId (null detaches) + publishTarget; create_channel takes publishTarget and writes the pointer; get_channel_config returns a `subchannel` block (isSubchannel, derivedFromChannelId, youtubeAuthChannelId, publishTarget, parentName); validateSubchannelParent rejects a missing/self/already-derived parent and a channel that has children (one-hop auth). The COCKPIT still has no subchannel UI (MCP-only) — that remains Phase 4 alongside derive_shorts, which is separately deferred and NOT a prerequisite: a subchannel's value for ORIGINAL authored short-form is its own config scope. 12 + 7 unit tests; core tests + core/worker/cockpit typecheck + cockpit prod build pass. Guide (both mirrors) + HANDOFF + BACKLOG + spec updated.",
    nextStep:
      "Operator: (1) migration 0069 must deploy (worker preDeploy) before youtube_auth_channel_id exists. (2) No live behavior changes on deploy — this is plumbing; the observable effect only appears once a subchannel is created (Phase 4) and its Short uploads to the parent's channel. Deterministic auth-resolution logic is covered by the 12 unit tests, so this can close on the tests + confirming the migration applied (get_diagnostics / table column present). As of #104 a connector RECONNECT IS needed for the new create_channel/set_channel_config params and the get_channel_config `subchannel` block. Verify (≤30s): get_channel_config on a normal channel → subchannel.isSubchannel false; then create_channel (or set_channel_config) with derivedFromChannelId = the parent + contentFormat 'short' → get_channel_config shows publishTarget 'parent-youtube' and youtubeAuthChannelId = the parent id, and authoring a ~2-min Short on it produces no below_midroll_floor advisory. Pointing it at a channel that is itself a subchannel must ERROR rather than store.",
  },
  {
    key: "force-forward-publish-and-upload-limit-guard",
    title: "force_forward = forward-only Publish-what's-built + YouTube upload-limit halt (Pentimento incident)",
    ticket: "operator-incident-2026-07-31",
    status: "shipped_pending_verification",
    summary:
      "SHIPPED (2026-07-31/08-01): a Pentimento ~45-min essay had a completed render but no clean way to publish it. force_forward now (1) accepts halted/scheduled/ready, (2) reuses every built artifact + skips variation under bypassChecks so a re-run makes ZERO new LLM/generation calls and no re-render (verified live: cost held at $1.02), (3) is FORWARD-ONLY — under bypassChecks it skips both human gates (visuals + final) and drives straight to upload+publish instead of dropping the production back to a gate (the force-forward click IS the approval, logged); cockpit button relabelled 'Publish what's built'. (4) New isTerminalUploadLimit() + UPLOAD_LIMIT_HALT_MESSAGE in upload-errors.ts: the upload step now HALTS on YouTube uploadLimitExceeded (the per-account daily upload-COUNT cap) instead of letting Inngest retry — every retry/force-forward was burning the cap even though nothing published. Core 406 tests + core/worker/cockpit typecheck + cockpit build pass.",
    nextStep:
      "Operator: the Carl Jung production is on_hold on the daily upload cap (uploadLimitExceeded). The cap resets at midnight US Pacific (~5pm AEST) — do NOT retry before then (each attempt counts). After the reset, open the production and click 'Publish what's built' ONCE: confirm it reuses the render (no re-render, cost unchanged), skips the gates, and uploads (providerVideoId + url populate). If it fails again with uploadLimitExceeded, it should now halt on_hold with the clear message rather than retry-burning. Deterministic parts (the error detector, the forward-only gate-skip) are unit/typecheck-covered.",
  },
  {
    key: "mcp-call-receipts-and-author-script-alternatives",
    title: "MCP call receipts (get_diagnostics.mcpCalls) + operator-authoring paths that don't need author_script (#88 append)",
    ticket: "01KYVE4AAY7N28H09XXQM1CPQQ",
    status: "shipped_pending_verification",
    summary:
      "#88 APPEND SHIPPED (2026-08-01): the operator appended that a FOURTH tool (get_production) now returns the same bare `No approval received`, which kills the ticket's own tool-annotation hypothesis — get_production has been in READ_ONLY_TOOLS all along, so the failing set does not correlate with the advertised hint (list_characters and set_channel_config are NOT hinted and succeeded). The remaining theories (host-side consent, session age, cumulative call count, payload size) were all indistinguishable from the client, and the ticket's open question — did the call ever reach the server? — had no answer. FIX 1, make it answerable: new mcp_call_log table + get_diagnostics.mcpCalls, an append-only RECEIPT for every MCP call that reaches the server (tool, ok, error, durationMs, argsBytes, at) plus lastHandshakeAt/lastToolsListAt from the initialize/tools-list handshake. No row for a failing tool = the call never arrived (host-side, unfixable here); ok:true = we answered and the reply was lost in transit; ok:false = it's genuinely ours and the error names it. Recording is best-effort and swallowed on failure, so a diagnostic can never break the call it observes; argument CONTENT is never stored, only its byte size (what a payload-limit theory needs). lastToolsListAt also separates 'not deployed' from 'stale cached tool list'. FIX 2, remove author_script as the single choke point (the ticket's §2 — with it blocked, every route to a finished episode runs the platform's own writer, forcing the visual-selection surface #65 is about): edit_script_beats now takes a SPARSE beats[] of per-index edits, so there is no beat-count matching (blocker a — the old failure was the browser string 'Segment count mismatch — reload and try again', which names no count; the legacy texts[] path now reports the actual beat count and points at beats[]), and each edit carries the VISUAL direction — imagePrompt, imagePrompts[] (#69 per-shot fan-out: how ~70 shot prompts are authored from ~16 beats), referenceEntity, visualBrief, motionPrompt, animates (blocker b). A visuals-only edit does NOT recut the voiceover. FIX 3 (the ticket's §3): new edit_shot_prompts(productionId, shots[], regenerate) — bulk shot-prompt replacement at the visuals gate, sparse by shotIndex; `regenerate` is REQUIRED and is the spend decision (false stores prompts only, free and nothing redrawn; true queues an async durable redraw job per shot, #83, one at a time per production, one jobId each — so a bulk pass can't time out mid-flight or be blind-retried into a double-bill, #66). Authoring at the SCRIPT gate is the cheaper path and the guide says so: direction lands before any image is generated. Both guide mirrors + HANDOFF + BACKLOG updated. 13 new unit tests; typecheck + prod build pass.",
    nextStep:
      "Operator: (1) MIGRATION 0068_mcp_call_log must deploy (worker preDeploy) before mcpCalls returns anything — until then it is an empty list, by design, not an error. (2) After a connector RECONNECT, reproduce a `No approval received` failure, then call get_diagnostics and read mcpCalls: no row for that tool = the call never reached us and the fault is host-side (raise with Anthropic, not as a platform ticket); a row means it did and the row says what happened. lastToolsListAt should show your reconnect. (3) The authoring workaround needs no live YouTube: greenlight an idea, wait for script_review, then edit_script_beats(productionId, beats:[{index:0, text:'…', imagePrompt:'…', referenceEntity:'B-47 Stratojet'}]) — confirm it succeeds WITHOUT matching the beat count, that get_production shows the authored prompt/entity on beat 0, and that the response says narrationChanged/visualsChanged. (4) At the visuals gate, edit_shot_prompts(..., regenerate:false) should store prompts with zero spend (verify in get_production_shots), and regenerate:true should return one jobId per shot. Deterministic parts are covered by unit tests, so this can close on the tests plus (2) and (3) if a live production isn't handy.",
  },
  {
    key: "authoring-path-approval-annotations",
    title: "review_beat_map auto-runs (compliance pre-check unblocked); author_script gates by design; get_channel_analytics hang-guard (#88)",
    ticket: "01KYVE4AAY7N28H09XXQM1CPQQ",
    status: "shipped_pending_verification",
    summary:
      "#88 SHIPPED (2026-07-31): the operator reported get_channel_analytics, review_beat_map and author_script all failing with the bare `No approval received` while every other tool on the connector worked, blocking the authored path. Grounding in code: `No approval received` is NOT a string anywhere in this repo — it's emitted by the Claude app, and no production/billing resulted, so the call was rejected at the HOST approval step before reaching the server. The server's only lever is the tools/list annotation. Diagnosis corrects the ticket's hypothesis: (a) get_channel_analytics has carried readOnlyHint since 2026-07-24 (commit b5fedf4, on main), so it was NOT missing the hint — it's the only read-only tool that makes a live external call (YouTube Analytics), so an auto-run that hangs has no fallback; (b) author_script SPENDS + creates a production, so it correctly requires an explicit approval — the fix is to grant it, not to auto-run a spending tool; (c) review_beat_map is the compliance/structural pre-check, is deterministic (reviewBeatMapDeterministic, no model call), touches no external system and only appends one audit row — gating it behind an approval the host wasn't surfacing left the compliance check unreachable. FIX: review_beat_map added to READ_ONLY_TOOLS so the app auto-runs it (annotation-only; execution unchanged, it still logs the row); get_channel_analytics's external call bounded by withTimeout(20s) → degrades to the stored-snapshot distribution on a hang instead of stalling the auto-run. New withTimeout util in @ytauto/core (5 tests). Both guide mirrors document the approval model. Typecheck + prod build + 389 core tests pass.",
    nextStep:
      "Operator, AFTER a connector reconnect (the readOnlyHint change ships in the cached tools/list): call review_beat_map — it should now run WITHOUT an approval prompt (like the other read tools). For author_script, expect and GRANT the approval prompt; `No approval received` means the host prompt wasn't actioned (it's an app-side message, not a platform reject). If author_script's prompt genuinely never renders in your host, that's a Claude-app/connector issue to raise with Anthropic — the platform correctly gates a spending tool. No migration.",
  },
  {
    key: "author-script-profile-merge",
    title: "author_script productionProfile partial-merge + resolvedProfile echo (#80)",
    ticket: "01KYTMKH0X1SB6S2MT7VQXA0HM",
    status: "shipped_pending_verification",
    summary:
      "#80 SHIPPED (2026-07-30): author_script's per-video productionProfile REPLACED the channel's stored profile wholesale — sending one axis (e.g. minSecondsPerShot) silently reset motion + all four image engines + voiceModel + everything else to platform defaults (a whole-video quality regression, invisible in the response). Root cause: the call site used `normaliseProfile(input) ?? resolve(stored)`, so ANY caller override became the ENTIRE profile. FIX: new pure mergeProductionProfile(stored, override, opts) in production-profile.ts (spread override over stored, then resolve — mirrors set_channel_config's partial write); the call site (mcp-authoring-actions.ts) now merges. The author_script response also returns resolvedProfile {motion, imageEngine, heroImageEngine, characterImageEngine, thumbnailImageEngine, voiceModel, music, captions, archivalStrength, visualDirector} so a caller can assert the engines instead of inferring them from shot-plan notes. 4 new unit tests (single-axis keeps the rest; override wins; no-override == resolve(stored); empty stored + override). Both guide mirrors updated. Typecheck (core/cockpit/worker) + 366 core tests pass.",
    nextStep:
      "Operator (after a connector reconnect for the new resolvedProfile field): author_script with productionProfile:{minSecondsPerShot:14} on a channel whose stored profile has motion:partial + imageEngine:seedream, and confirm the response's resolvedProfile still shows motion:partial + seedream (not static/qwen) — and that the rendered video uses seedream. No migration; the merge is in the authoring action.",
  },
  {
    key: "published-status-and-publication-surface",
    title: "Published production kept stale on_hold failureReason; get_production surfaces the publication; wordBasedDurationSec (#81)",
    ticket: "01KYTN0XTV3KMY04KHD1TQC78H",
    status: "shipped_pending_verification",
    summary:
      "#81 SHIPPED (2026-07-30): a production that timed out at a gate (→ on_hold + 'visuals_review gate timed out') then published still read status:'on_hold' with the stale failureReason, so an agent/operator concluded nothing published. Root cause: the worker setStatus only ever WROTE a failureReason (never cleared) and the publish finalize-publication step set status:'published' without clearing it. FIX: new pure productionStatusPatch(status, reason?) in gate-lifecycle.ts (a transition CLEARS failureReason unless a new one is passed — verified all 14 off-ramp setStatus callers pass one) used by setStatus + finalize-publication. get_production now returns the publication (id, providerVideoId, url, privacyStatus, publishedAt, scheduledFor) + a statusMismatch flag (live video on an on_hold/failed/rejected row) so the contradiction is visible in the same tool. Secondary (runtime projection): shotPlan gains wordBasedDurationSec (this script's own runtime at 2.5 w/s, independent of the channel target that estimatedDurationSec echoes) + a >25% divergence note. 6 new unit tests. DEFERRED: (1) changing WORDS_PER_SEC 2.5→~2.7 — the projection is only ~8% off from the real spoken rate, and it's a platform-wide budget/advisory change to make with the operator present. (2) a gate-reopen-after-timeout recovery tool — the operator WITHDREW the recovery framing (the video published), and it overlaps the existing reopen-visuals-gate deferred item. NOTE: existing prod rows already published+stale are NOT retro-fixed by code — they need a one-time manual clear / reconcile; the fix prevents recurrence.",
    nextStep:
      "Operator (after a connector reconnect for the new get_production.publication field): get_production on a live production and confirm publication.url + publishedAt appear; for the specific stale row 01KY6DN3EYT3SW9JNY0297GJDE, confirm publication.statusMismatch:true flags it (and clear its status manually — code only prevents NEW occurrences). Same-commit deploy signal: the resolvedProfile field on author_script (#80) landing over MCP proves this build deployed.",
  },
  {
    key: "thumbnail-source-and-unfreeze",
    title: "Sourced thumbnail base (#74) + unfreeze thumbnail post-gate + live swap (#76)",
    ticket: "01KYK11TCZ5K4K7F0TGYV4NP50,01KYKHFZVEPQBWMK1G36HEJEY0",
    status: "shipped_pending_verification",
    summary:
      "#74 SHIPPED: regenerate_thumbnail now takes referenceEntity → sources a real archival photo of that subject (providers.reference.findEntityImages, the same path regenerate_shot's re-source uses), up to 3 auto-credited candidates, so the thumbnail (the one image that most needs a real photo) is no longer generate-only. #74 APPEND SHIPPED: referenceImages (url[]) → operator-supplied image-CONDITIONED generation (pass-through to generateImage's referenceImageUrl/extraReferenceImageUrls/referenceStrength — already supported by gemini/qwen/seedream), so text-to-image that can't render a specific 1950s airframe now generates FROM the operator's real photo while thumbnailPrompt drives composition. #76 SHIPPED: the thumbnail is no longer frozen at gate approval — regenerate_thumbnail runs at thumbnail_review AND while ready/scheduled/published; new tool set_video_thumbnail(productionId, {thumbnailId?}) pushes a chosen candidate to the live/scheduled YouTube video via the existing providers.publish.setThumbnail (thumbnails.set), a one-call swap. set_publication_schedule(cancel:true) is documented to park the video private (status→published), NOT reopen the gate. Typecheck + build verified.",
    nextStep:
      "Operator (live, after connector reconnect): on a scheduled/private production, regenerate_thumbnail({referenceEntity:'<subject>'}) → confirm a sourced candidate appears; set_video_thumbnail(productionId) → confirm the thumbnail changes on YouTube (needs the thumbnails.set OAuth scope — re-consent the channel if it 403s). No live YouTube API from the sandbox, so this is code-verified only.",
  },
  {
    key: "thumbnail-compositor",
    title: "Thumbnail compositor — real type engine + shape/band layers over a base image (#75)",
    ticket: "01KYK2310E8EHY2E6S895TDT1H",
    status: "deferred",
    summary:
      "#75: thumbnails are a SINGLE diffusion image — every element (subject, type, arrows) must emerge from one prompt, and the model garbles longer text + can't kern/place it. The winning thumbnails in the niche are COMPOSITED: photographic/cut-out subject + flat graphic ground + a separately-set typographic layer. Ground truth: there is NO compositing/text-overlay layer today (thumbnail-compose.ts builds a PROMPT, not a canvas; overlay text is baked into the diffusion prompt string in thumbnail-prompts.ts:96). sharp IS available (packages/providers) but used only to JPEG-normalize at push (publish.ts toYouTubeThumbnail). The fix is a real compositor: render/source the base as now, then composite a deterministic text layer (string, font, weight, size, colour, outline/stroke, x/y, optional solid band) with a real type engine (sharp + an SVG text layer), driven by regenerate_thumbnail fields + productionProfile.thumbnailTemplate; plus optional shape (arrow/ring/rule) and background-replacement/cut-out so a sourced airframe sits on a flat brand ground. DEFERRED as operator-present: the whole value is the VISUAL output (typography/placement/legibility at 320x180), which can't be verified from the sandbox — build it where the operator can review renders. The SVG-layer builder is pure and should ship with unit tests; the composite is the visual check.",
    nextStep:
      "With the operator present: add a pure SVG text-layer builder (unit-tested) + a sharp compositor in packages/providers, wire a caption/textLayer field onto regenerate_thumbnail and a channel default onto thumbnailTemplate, render samples, and check legibility against the reference thumbnails. Start with the text layer (the bulk of the gap), then shape + background-replacement.",
  },
  {
    key: "caption-style-and-quote-card",
    title: "Caption style object + per-phrase emphasis + quote-card beat type (#72)",
    ticket: "01KYGFBQ0RJEG8ZZWA92JCG628",
    status: "shipped_pending_verification",
    summary:
      "#72 SHIPPED (2026-07-27): captions were hardcoded lower-third TikTok style. Added productionProfile.captionStyle {position:lower-third/center/upper-third, casing:as-written/upper/sentence, typeface:sans/serif/slab, weight:400-900, outline, maxLines, emphasisColor, emphasisPhrases} — resolved by a pure, unit-tested core module (caption-style.ts: resolveCaptionStyle/applyCasing/emphasizedWordIndices), applied in Captions.tsx. serif/slab load real Google fonts (PlayfairDisplay/RobotoSlab) so they render on Lambda. Per-phrase emphasis is done by PHRASE MATCHING against the caption word stream (emphasisPhrases coloured wherever they appear) rather than an inline narration marker — no TTS-strip fragility, and it's what delivers 'ARE NOT LIBERATED in red'. Quote-card device shipped as beats[].quoteCard {text, attribution?}: a QuoteCard Remotion component renders typeset text on a near-black ground in place of the image (wired through ScriptBeat/BeatInput/Shot/planShots/shortProps/author_script). All behaviour-preserving: unset captionStyle + no quoteCard = today's render, byte-identical. Typecheck (db/core/video/worker/cockpit) + unit tests + cockpit prod build pass.",
    nextStep:
      "Operator (live render check): set captionStyle {position:center, casing:upper, typeface:serif, emphasisColor:#C1121F, emphasisPhrases:[...]} on the channel and author a beat with quoteCard, render a sample, and confirm against the reference frames (centred ALL-CAPS serif, the emphasis phrase coloured, a typeset quote card at the section boundary). No migration — captionStyle lives in the channel DNA productionProfile jsonb, quoteCard in script_drafts.beats. The inline-narration emphasis MARKER (vs the phrase list shipped) remains the one deferred sub-item; open a follow-up if phrase-matching proves insufficient.",
  },
  {
    key: "caption-legibility",
    title: "Caption legibility — base colour + outline/shadow/scrim, reject unknown keys (#79)",
    ticket: "01KYSA67QN5Z1DNZKRJ9RZS0HZ",
    status: "shipped_pending_verification",
    summary:
      "#79 SHIPPED (2026-07-30): captions vanished over bright imagery. Root cause: base text was white with only a SOFT shadow and no outline by default, active/emphasis words took the low-contrast brand colour, captionStyle had no base-colour/outline/shadow controls, AND unknown keys were silently dropped (the color/outlineColor/outlineWidth/shadow the operator sent returned ok:true and were discarded). Fix (caption-style.ts + Captions.tsx renderer + production-profile/beats/db schemas): (A) new fields color (default white), outlineColor (default black), outlineWidth (0-12px, default 4 heavy; 0 or outline:false disables), shadow (default true), scrim (dark band, default false). (B) DEFAULT is now white + heavy dark outline (paintOrder stroke-under-fill) + strong shadow. (C) optional scrim band. (D) the captionStyle schema is .strict() — unknown keys REJECTED with an error naming the key + listing accepted fields (normaliseProfile). (E) emphasisColor is wired but only colours emphasisPhrases matches (documented; the operator's cyan was the brand accent on the active word). COMPOSITION_BUNDLE_MIN_DATE bumped to 2026-07-30 so the fail-loud guard requires a fresh Lambda bundle. UPDATE 2026-07-31 (operator re-tested: paint fields stored but NOT rendered — captions came out BLUE not white, no scrim, thin outline, while position/casing/typeface WERE honored): root cause found + fixed in Captions.tsx — (1) the currently-spoken ('active') word was HARDCODED to the brand accentColor, overriding the configured `color`, so every spoken word rendered blue; now the active word uses the base `color` and a new OPT-IN `activeColor` field gives a coloured karaoke highlight (default null = base color). (2) outline (WebkitTextStroke) + shadow lived only on the container, so the per-word spans (which re-declare `color`) dropped them via unreliable inheritance — now applied PER WORD, so a configured 9px outline/shadow actually renders. COMPOSITION_BUNDLE_MIN_DATE re-bumped to 2026-07-31 (requires a fresh Lambda bundle again). 384 core tests (activeColor coverage added). This CHANGES the default: the active word is no longer brand-accent — channels wanting that set activeColor.",
    nextStep:
      "Operator (live render, after the Lambda site redeploys on THIS packages/video push): re-render Pentimento 01KYRBCPPPQC… with captionStyle {color:'#FFFFFF', scrim:true, outlineWidth:9} and confirm captions now render WHITE with a real outline + scrim band (not blue). To restore a coloured karaoke highlight set activeColor. Confirm the earlier scrim/outline now actually apply. If it still renders blue, the Lambda site bundle didn't redeploy — check deploy-lambda-site CI.",
  },
  {
    key: "publish-schedule-sync",
    title: "Publication schedule sync + drift correction",
    ticket: "01KY9C9RFR3J39MSYBZRASYAP3",
    status: "shipped_pending_verification",
    summary:
      "publishedAt drift is now detected + corrected: reconcile_publications flags a live record whose stored date disagrees with YouTube's real publishedAt by >1h and, under fix:true, rewrites it and re-triggers analytics ingest on a backward move; the publish-finalize cron now stamps YouTube's real go-live time (not the future slot) when a scheduled video is released off-slot; and set_publication_schedule / sync_publication_from_youtube expose scheduling + external-publish over MCP. All typecheck/build/unit-test verified only — no live YouTube API or prod DB from the sandbox.",
    nextStep:
      "Operator: reconnect the connector (new tools), then on production 01KY3B8ANSJR7150Z3BWKMXCTA run sync_publication_from_youtube (or reconcile_publications fix:true) to pull the real publishedAt for video 5sNT9OFv6DY; confirm get_video_analytics then shows a past publishedAt + dataState≠none after the next analytics-ingest cycle (YouTube's 24-72h lag applies).",
  },
  {
    key: "analytics-phase1-verify",
    title: "Analytics Phase 1 — live verification",
    ticket: "01KY1VEZ094TRVH8G06JX4MJVR",
    status: "shipped_pending_verification",
    summary:
      "Retention curve + watch/engagement/traffic reports are wired and unit-tested, but only populate on the NEXT analytics-ingest run (+ YouTube's 24-72h lag). get_video_analytics reads the latest snapshot, which is pre-deploy until then.",
    nextStep:
      "After the next analytics-ingest, check get_video_analytics coverage. If retentionCurve is still false, re-consent the channel with the yt-analytics.readonly scope.",
  },
  {
    key: "analytics-phase2",
    title: "Analytics Phase 2 — portfolio + scheduled refresh",
    ticket: "01KY1VEZ094TRVH8G06JX4MJVR",
    status: "deferred",
    summary:
      "get_portfolio_analytics + cost-per-1k-views join; scheduled tiered refresh with per-metric fetchedAt. CORRECTION (2026-07-28): impressions + CTR are NO LONGER Studio-only — YouTube added videoThumbnailImpressions + videoThumbnailImpressionsClickRate to the Analytics API on 2026-01-15, and we now FETCH them (packages/providers/src/real/analytics.ts, fail-soft report), STORE them (analytics_snapshots.impressions/ctr, columns already existed), roll them up (channelPerformanceSummary/videoPerformance + coverage.impressionsCtr), and DISPLAY them on the channel Analytics tab + get_channel_analytics/get_video_analytics. So the Reporting API bulk-export path is NOT needed for impressions/CTR. They populate on the next analytics-ingest, subject to YouTube's 24-72h lag on new videos.",
    nextStep: "Portfolio rollup + scheduled refresh remain the Phase-2 work; impressions/CTR are done (verify live on the next ingest).",
  },
  {
    key: "alert-selfheal-effect",
    title: "Alert self-heal — retroactive clear",
    ticket: "01KY1SX298DQW956GE7N38BCJ3",
    status: "shipped_pending_verification",
    summary:
      "The min-sample gate stops NEW criticals immediately; the three existing critical alerts auto-ack on the NEXT analytics-ingest run, not on deploy. get_diagnostics shows them open until that runs.",
    nextStep: "Confirm the three underperformance alerts clear after the next analytics-ingest cycle.",
  },
  {
    key: "beatmap-pipeline-gate",
    title: "Beat-map reviewer — pipeline hard-block + cross-model LLM",
    ticket: "01KY1Y9E1H2QF2CNJNECVNXREW",
    status: "deferred",
    summary:
      "review_beat_map (deterministic checks + loop controls) ships opt-in. The pipeline pre-authoring gate that HARD-blocks a production, and the cross-model LLM advisory layer, are default-off — they change live production behaviour.",
    nextStep: "Enable with the operator present, after confirming it doesn't wrongly halt real productions; wire the model config to OpenRouter.",
  },
  {
    key: "prompt-editing",
    title: "Prompt dashboard — editing + versioning",
    ticket: "01KY1X58XSCY27SD903Z4H73JC",
    status: "deferred",
    summary:
      "get_agent_prompts + /prompts ship read-only. Full prompt-text viewing, version history, diff-against-default and editing require centralising the inline system: prompts out of ~25 agent files — a cross-agent refactor.",
    nextStep: "Do the prompt-centralisation refactor with the operator present (it touches compliance-relevant agents).",
  },
  {
    key: "image-dedup-pipeline",
    title: "Image dedup — perceptual hash + cross-production",
    ticket: "01KY1ZNPT18X6CR3EZNN6FN1ZB",
    status: "deferred",
    summary:
      "Authoring-time advisory (repeated referenceEntity) shipped. The pipeline-side perceptual-hash + cross-production dedup is ABSORBED INTO the media-library epic (GitHub #26) — same substrate (queryable asset store + usage tracking + hashes).",
    nextStep: "Build as part of the media-library epic (media-library-epic).",
  },
  {
    key: "slate-gate-enforcement",
    title: "review_slate — hard gate on write_idea/create_series + revision loop",
    ticket: "01KY2BJ9YM7GPHMKB6K9NDNHWC",
    status: "deferred",
    summary:
      "review_slate ships standalone + opt-in (advisory, like review_beat_map) — it does NOT yet auto-block write_idea/create_series, because auto-rejecting ideas from the backlog changes live behaviour and must be enabled with the operator present. The slate-revision loop (reuse runReviewLoop for the block→revise→re-review cycle) is the second deferred piece. Also flagged by the ticket: audit which other DNA/charter fields are SET but never TESTED by any gate (forbiddenTopics is now tested at slate + production; others may not be).",
    nextStep:
      "With the operator present: wire review_slate as a pre-write hard gate on write_idea/create_series (block authority on forbiddenTopics), add a runReviewLoop-bounded slate-revision loop, and audit untested config fields.",
  },
  {
    key: "branding-authoring-over-mcp",
    title: "Channel branding — authored regeneration over MCP",
    ticket: "01KY2A8HRGSPSAP5NBY7EZQQ3T",
    status: "deferred",
    summary:
      "get_channel_branding (read) + an honest create_channel checklist shipped. The authored regenerate path (set_channel_branding with a verbatim avatarPrompt/bannerPrompt, mirroring the imagePrompt rails) is deferred: it spends on image generation and needs the square-avatar (800x800) + banner safe-area (central ~1235x338) composition the ticket specifies, best enabled deliberately. Generation exists today in the cockpit (Settings -> Branding).",
    nextStep:
      "Wire an MCP set_channel_branding to the existing generate actions with verbatim-prompt rails + explicit square/safe-area composition; enable with the operator present (it's a spend path).",
  },
  {
    key: "content-driven-runtime-consumption",
    title: "Content-driven runtime — per-production target consumed by author_script + assembly",
    ticket: "01KY61RCNZSHVG93P865M3K2D4",
    status: "deferred",
    summary:
      "SHIPPED the safe slice of #39: a lengthPolicy DNA field (floorSec hard 480 = mid-roll threshold, ceilingSec soft, named bands, principle) resolved with defaults, settable via set_channel_config and returned by get_channel_config; and an ADVISORY (never-block) runtime↔depth check in review_beat_map that flags padding/cramming and the mid-roll floor. targetLengthSec stays the runtime anchor — nothing in the pipeline changed. DEFERRED: making a PER-PRODUCTION runtime target (chosen at beat-map time) actually drive author_script + assembly instead of the channel default, and turning the floor into a hard bound — that changes live production runtime, so it's built default-off with the operator present.",
    nextStep:
      "With the operator present: thread a per-production runtimeSec (set at review_beat_map / author_script) through to assembly, and decide whether floorSec becomes a hard pre-publish check. Confirm the advisory bands are calibrated against real retention first.",
  },
  {
    key: "reopen-visuals-gate",
    title: "Reopen the visuals gate — recover a stranded shot-fix pass",
    ticket: "01KY6DCDXYP37VS4D03QTAJNNT",
    status: "deferred",
    summary:
      "regenerate_shot is gated to visuals_review, so a shot-fix pass that spans sessions is stranded if the gate is approved between sittings — the production advances to thumbnail_review and the remaining bad shots can only be shipped or fixed by re-authoring the whole video. SHIPPED the visibility half now: get_production_shots + get_gate surface outstandingDuplicateShots + duplicateRiskGroups (shots sharing a referenceEntity) so the operator sees what's unfixed BEFORE approving, and regenerate_shot's out-of-state error names the current status + recovery path. DEFERRED: the actual 'Revise visuals' operator action that returns a production thumbnail_review -> visuals_review, because it re-fires the Inngest pipeline mid-flight (reopen the visuals gate, cancel the thumbnail gate/state, re-run finalize->thumbnail after re-approval) — a live-behaviour change that must be built default-off with the operator present. Approval stays a cockpit action; MCP only needs to SEE it's possible and call regenerate_shot once reopened.",
    nextStep:
      "With the operator present: add a cockpit 'Revise visuals' decision on the final gate that reopens visuals_review and safely re-drives the pipeline from image-finalize; keep gate approval human-only. The durable fix is within-production sourcing dedup (media-library-epic), which removes most of these groups before they need hand-fixing.",
  },
  {
    key: "channel-strategy-agent-reading",
    title: "Channel strategy — opt-in reading by the ideation + slate agents",
    ticket: "01KYDZKW7BH2T9P4KBCTYMNDH9",
    status: "deferred",
    summary:
      "SHIPPED the durable store (#61): a high-capacity, section-scoped, timestamped channelStrategy document on the channel (jsonb column, migration 0065), with get_channel_strategy / set_channel_strategy over MCP. It is deliberately NOT read by the authoring pipeline — the whole point is a home for strategy that never pollutes a script/image/thumbnail prompt. DEFERRED the ticket's point 4: having the IDEATION and SLATE-REVIEW agents specifically read it (so they know the taxonomy + which clusters are covered) — that changes what those agents generate/flag (live behaviour) and needs the operator present to enable + calibrate, plus real-LLM verification the sandbox can't do. Ship it as a default-off per-agent opt-in.",
    nextStep:
      "With the operator present: thread the strategy doc (or selected sections) into the ideation prompt (packages/agents/src/ideation.ts) and the slate-review context as an opt-in, default-off input; verify against a real LLM run that it improves coverage-awareness without bloating token cost.",
  },
  {
    key: "producibility-mfk-and-ideation",
    title: "Producibility — MFK publish read-through (manual paths) + ideation-side constraint",
    ticket: "01KY9F0KMQM9FXHVZJ4GAG85XP",
    status: "deferred",
    summary:
      "SHIPPED the review_slate producibility DIMENSION (#54): flags ideas a faceless generative channel can't film (visualMode-gated) and rap/song/chant TTS can't perform. SHIPPED the Made-for-Kids storage + surface (#53): a channels.madeForKids column (migration 0066), set_channel_config + get_channel_config, a consistencyWarnings check (undeclared-kids-channel + charter objectives that depend on end-cards/comments MFK disables), a review_slate comment-CTA producibility rule gated on it, and the PRIMARY publish read-through — the two UPLOAD call sites (production-pipeline + publish-clip) send the stored madeForKids. UPDATE 2026-07-31: the MANUAL publish/schedule callers now ALSO thread it — releasePublicationAction, reschedulePublicationAction, cancelScheduledReleaseAction (cockpit actions.ts) and the MCP set_publication_schedule tool all re-send channel.madeForKids so a chat/cockpit 'publish this scheduled video now' or 'reschedule it' no longer strips the COPPA designation (the videos.update replaces status wholesale). So the release/reschedule half of piece (1) is DONE. STILL DEFERRED: (2) constraining the IDEATION agent at source (feed it charter mission + visualMode + madeForKids so it stops generating unproducible/comment-CTA ideas) — changes live generation, ships with the operator.",
    nextStep:
      "Operator (live, after connector reconnect): publish a scheduled MFK video via release_publication (or reschedule via set_publication_schedule) and confirm on YouTube the video stays declared Made-for-Kids. Then (deferred) thread charter mission + visualMode + madeForKids into the ideation prompt (packages/agents/src/ideation.ts) with the operator present.",
  },
  {
    key: "flat-run-duration-report",
    title: "review_beat_map flat_run reported total runtime, not the span's elapsed time (#82)",
    ticket: "01KYTPZTKRFQWFH533BVMDWVJG",
    status: "shipped_pending_verification",
    summary:
      "#82 SHIPPED (2026-07-31): flat_run's evidence said '16.8 min with no re-hook' for a span whose beats sum to 5.0 min — it reported the whole runtime, and rendered the interval as '~4-4 min'. Root cause in beatDurationsSec (beat-map.ts): it assumed timingSec is ALWAYS cumulative-from-start, so a map supplying PER-BEAT durations made the last beat absorb targetLengthSec−lastValue, ballooning a tail span to ~the whole runtime. Fix: infer the shape — cumulative offsets are monotonic AND sum to ≫ runtime (deltas), otherwise treat values as per-beat durations verbatim; and the interval string now renders '~3.5 min' (was '~4-4'). Unit-tested (per-beat vs cumulative durations; the span elapsed ≈ 5 min not 17; the clean interval string). Deterministic — closes on the tests.",
    nextStep:
      "Operator (after a connector reconnect): re-run review_beat_map on the reported 31-beat Lost Books map and confirm flat_run now reads the span's own ~5 min and '~3.5 min' interval, not '16.8 min' / '~4-4 min'. Closes on the unit tests without a live render.",
  },
  {
    key: "beat-visual-brief-supply",
    title: "One visual brief per beat, but the shot planner cuts more shots than beats (#69)",
    ticket: "01KYE-beat-visual-brief",
    status: "shipped_pending_verification",
    summary:
      "#69: a beat carried exactly one referenceEntity, but the shot planner cuts more shots than beats, so an artwork-driven channel couldn't supply enough distinct visual briefs and review_beat_map returned contradictory advisories. SHIPPED (2026-07-27): beats[].referenceEntities (an ordered list) is now on the beat-map schema, ScriptBeat, BeatInput, author_script + review_beat_map inputs; planShots + planShotsFromDirection consume it in order across a beat's shots (shot i → referenceEntities[i], fallback referenceEntity then null), so a beat that fans into N shots supplies N distinct subjects without inflating the beat count. review_beat_map's shotEstimate now returns suppliedEntities + entityCoverage (the coverage the operator was measuring by hand). The runtime_compressed_for_beats carve-out for motion:static + imageDensity:relaxed also shipped (a high beats/min there is a shot-supply strategy, not cramming). The earlier advisory-calibration half (payoff_position marker + flat_run elapsed-time) shipped separately in c019479. UPDATE 2026-07-31 — the GENERATED-shot half of the append shipped: beats[].imagePrompts[] (the generative twin of referenceEntities) is now on ScriptBeat, BeatInput and author_script; planShots + planShotsFromDirection consume it in order (shot i → imagePrompts[i], fallback imagePrompt), so a GENERATED beat that fans into N shots renders N distinct images instead of the same prompt N times. Also shipped the minSecondsPerShot-inert-while-animating warning: minSecondsPerShotOverrideWarning() (pure, unit-tested) surfaces on set_channel_config AND in shotPlan.notes when the floor is set above the ~10s i2v clip cap while motion animates (raising it saves no shots — the operator's #2 append). Typecheck + prod build + unit tests (planShots distribution for both lists, override warning, entityCoverage, carve-out) verified; live render is the operator's check.",
    nextStep:
      "Operator (after a connector reconnect): author_script a GENERATED beat with beats[].imagePrompts[] and confirm the rendered sibling shots use distinct prompts (no duplicate diagrams at the visuals gate); and set minSecondsPerShot > 10 on a motion:ai_video channel → confirm set_channel_config returns the inert-floor warning. No migration needed — beats persist in the existing script_drafts.beats jsonb.",
  },
  {
    key: "per-shot-animate-and-ideation-format-gate",
    title: "Per-shot animate over MCP (#70) + ideation reading its own format rules (#68B)",
    ticket: "01KYE-animate-ideation-format",
    status: "deferred",
    summary:
      "#70: regenerate_shot now casts a character (characterId) over MCP, but there's still no per-shot ANIMATE over MCP — generate/replace/remove a motion clip for one shot from an authored motionPrompt + videoEngine, the cockpit's per-shot Animate. Needs a new tool (or mode) wired to the clip-generation path (apps/worker clip-generate / clip-generation), cost-appending, gate-stays-open, human-approval preserved. #68B: automatic ideation should CHECK a proposed idea against the channel's own titleTemplates + charter.mission before writing it (it currently ignores them and writes off-format ideas). This is the same 'constrain ideation at source' work as producibility-mfk-and-ideation — run the review_slate deterministic checks (or a subset) inside the ideation cron before insert. Both change live generation/spend, so operator-present.",
    nextStep:
      "With the operator present: add a per-shot animate tool over MCP (verbatim motionPrompt + videoEngine, replace/remove a clip); and gate the ideation cron (trend-scan / scanTrendsForChannel) so a generated idea is checked against titleTemplates + charter before it lands in the backlog.",
  },
  {
    key: "regenerate-shot-reliability",
    title: "regenerate_shot — write serialization, generate-mode async/idempotency, gate-reentry clip cost",
    ticket: "01KYE-regenerate-shot-cluster",
    status: "deferred",
    summary:
      "SHIPPED the observable/auditable slice of the #63/#65/#66/#67 cluster: get_production_shots + get_gate now report assetType (still | generated_clip | sourced_clip) + clipProvenance + assetCounts so the gate is auditable and `animated` no longer conflates AI clips with real footage (#65/#67 reporting); get_production_shot reads ONE shot cheaply after a timeout (#66 shots); get_gate on a thumbnail_review gate + list_thumbnails now return the thumbnail CANDIDATES {id,url,prompt,engine,predictedCtr,selected,createdAt} so a timed-out regenerate_thumbnail is recoverable (a rising thumbnailCount / fresh createdAt = it landed; don't blind-retry) — the #66 append's read-back ask for the thumbnail twin; and swapShotImage no longer stores a shot's own narration as its imagePrompt (#63 minimum ask). DEFERRED the harder roots: (1) #63 — the deeper clobber is a cross-process race: the MCP-direct swapShotImageAction (runs in the cockpit) and the worker-queued shot-op (serialized only on the worker) both read-modify-write asset.meta with no shared lock, so a no-prompt op can overwrite an authored prompt; the fix is a per-production/per-asset advisory lock (or routing all shot ops through the worker queue). (2) #66 — generate mode blocks the MCP HTTP response on the inline image-engine round-trip and times out; the real fix is an async job handle + poll (like the cockpit's queueShotOpAction) plus an idempotency/request key so a retry doesn't double-bill — this applies EQUALLY to regenerate_thumbnail (#66 append: same timeout-completes-and-bills mode; the get_gate read-back above makes it recoverable, but blind-retry still double-bills two hero images until this lands). (3) #67 — a pipeline motion re-entry (retry-from-render / Inngest retry) regenerates EVERY planned-but-missing clip, not just the deleted one, and a clip DELETE doesn't bump updatedAt so the gate-skip guard misses it — real unrequested clip spend. (4) #65 cockpit — the visuals gate PAGE renders the still poster for a clip; making the preview a true poster frame of the clip is a cockpit-render change.",
    nextStep:
      "With the operator present (live-spend + concurrency, sandbox-untestable): serialize shot-op meta writes across the MCP + worker paths (advisory lock or worker-only queue); make generate-mode regenerate_shot async with a job handle + idempotency key; scope pipeline clip re-generation to only the intended shot on gate re-entry and make a clip delete visible to the gate-skip guard; and render a true clip poster-frame at the gate.",
  },
  {
    key: "async-mcp-jobs",
    title: "Async job handles for long MCP tools (#83) — fill_thin_prompts shipped, regenerate_thumbnail remains",
    ticket: "01KYTQ6EK9GW30WY6Z7WXM5EPA",
    status: "shipped_pending_verification",
    summary:
      "#83: regenerate_thumbnail and fill_thin_prompts held the MCP connection open while a worker/LLM job ran and routinely timed out, indistinguishable from failure, causing double-spend on retry. SHIPPED (2026-07-31): fill_thin_prompts is now ASYNC — it enqueues the existing shotJobs 'fill-prompts' worker op (shot-op.ts already handled it) via queueShotOpAction and returns { jobId, status:'running' } immediately; queueShotOpAction now returns the jobId; and a new read-only get_job(jobId) MCP tool polls shotJobs (status queued|running|done|failed, op, error, timestamps). So the worst offender (fill_thin_prompts, which fans out over many prompts and always timed out) is fixed and the async pattern + poll tool now exist. DEFERRED: making regenerate_thumbnail async the same way needs its generation logic (currently in the cockpit regenerateThumbnailsAction, not @ytauto/agents) extracted into a shared worker-callable op + a new 'thumbnail' shotJobs op + worker branch — a refactor of a live paid path, best done deliberately. Its documented recovery (list_thumbnails/get_gate rising count = it landed, don't blind-retry) still holds meanwhile. An idempotency-key column is now lower priority: returning a jobId immediately removes the timeout-then-retry that caused the double-spend. Typecheck + build + unit tests green.",
    nextStep:
      "Operator (after connector reconnect): call fill_thin_prompts → confirm it returns a jobId fast (no timeout); poll get_job(jobId) → done. Then (next change) extract thumbnail generation into a worker op and make regenerate_thumbnail async the same way.",
  },
  {
    key: "ideaid-episode-resolution",
    title: "review_beat_map accepted a series-episode id that author_script rejected (#86)",
    ticket: "01KYTZJG82VEQB2W1KJTR0PD8N",
    status: "shipped_pending_verification",
    summary:
      "#86 SHIPPED (2026-07-31): review_beat_map accepted a series-EPISODE id as ideaId (it only used it as an opaque comparison key) while author_script rejected the same id with 'ideaId not found' — so a map could pass review against an id that couldn't be authored, failing only after the full payload was built. FIX (operator's preferred option 2): a shared read-only resolveIdeaRef(db, id) now backs BOTH tools — it returns the id as-is when it's a real idea, resolves a series episode to its backing idea, or reports unknown. author_script accepts an episode id: it resolves to episode.ideaId, and if the episode isn't queued to one yet it MINTS an idea (sourceType 'editorial', like the episode-research handoff) and LINKS it (episodes.ideaId + status 'queued') — so the authored production ties to the arc episode and post-publish reconciliation (which matches by ideaId in editorial-postpublish, NOT by title — this also answers the ticket's 'related observation') marks the episode published. review_beat_map normalizes the same-episode key via the resolver and returns an ideaIdWarning up front when the id matches neither an idea nor an episode. A cut episode is rejected with a clear message. Typecheck + build verified; DB-resolution logic so the operator's repro is the live check.",
    nextStep:
      "Operator (after connector reconnect): author_script with a series episode id from list_series (e.g. the Comet episode) → confirm it succeeds and, after publish, the arc episode flips to published (was staying 'queued'). review_beat_map with a bogus id → confirm ideaIdWarning. No migration.",
  },
  {
    key: "legacy-schedule-escape-hatch",
    title: "Not-yet-uploaded scheduled production was un-reschedulable (#85)",
    ticket: "01KYTSDMY36QS9PFDYHY9ZEJ8B",
    status: "shipped_pending_verification",
    summary:
      "#85 SHIPPED (2026-07-31): a production scheduled through the OLD sleep-based pipeline (no providerVideoId) was stuck — release_publication refused ('not uploaded'), set_publication_schedule refused ('set it at the final review gate'), and the gate was already closed: a closed loop. FIX: set_publication_schedule now handles a NOT-YET-UPLOADED row as a purely LOCAL calendar write (update publications.scheduledFor + productions.status, no YouTube call since there's nothing to move there) for both (re)schedule and cancel, replacing the dead-end refusal. The response carries uploaded:false + a note that the row has no recorded upload so it won't go live until it's uploaded (retry_production) or reconciled — honest instead of pointing at a surface that no longer exists. Typecheck + build verified.",
    nextStep:
      "Operator (after connector reconnect): on the stuck Pentimento production, set_publication_schedule with a new scheduledFor → confirm it moves the slot (uploaded:false note). NOTE: to actually PUBLISH a legacy no-upload row still requires uploading it — that overlaps #87 (retry-from-render currently duplicates), which is the operator-live piece.",
  },
  {
    key: "duplicate-upload-and-silent-failure",
    title: "Retry-from-render duplicate uploads + silent upload failures (#87)",
    ticket: "01KYV5BHM54V5SZH5WFNGM0VAS",
    status: "deferred",
    summary:
      "#87: retry-from-render on production 01KYRBCPPPQC… initiated SEVEN new YouTube uploads (none completed — all stuck 'processing', likely quota-exhausted at ~7×1600=11,200 units > 10,000/day), the platform recorded NO providerVideoId for any, get_diagnostics showed nothing, and a duplicate PRODUCTION (01KYTSRE…) was minted too. SHIPPED (2026-07-31) the observability half: findSuspiciousPublications (→ get_diagnostics.publicationIssues) now flags STUCK UPLOADS (a production at scheduled/published with no providerVideoId, via UPLOAD_EXPECTED_STATUSES) and duplicate published/SCHEDULED productions for one idea — it only looked at status='published' before, which is exactly why the seven-scheduled-no-id case was invisible. DEFERRED (operator-live, sandbox-unverifiable — real uploads/quota/live YouTube processing states): (1) idempotent uploads — the pipeline orphan-adoption (findRecentUpload) requires an EXACT title match within 120min AND skips still-processing uploads (durationSec==null), so a retry after a title change / long gap / before processing finishes always re-uploads; the robust fix is matching on uploadStatus (adopt 'uploaded'/'processed', skip 'failed') + normalized title, and stamping productionId into the upload for a reliable link. (2) a retry-from-render preflight that refuses/​warns on an existing upload. (3) refuse an upload that would exceed remaining daily quota (needs live quota accounting) + raise a get_diagnostics alert on upload failure. (4) stop retry-from-render minting a duplicate PRODUCTION. All share the #84 record-divergence root and touch the live paid upload path — do with the operator present.",
    nextStep:
      "With the operator present (live YouTube + quota): make findRecentUpload adopt a still-processing recent upload by uploadStatus (not durationSec) + tolerate a changed title; add a retry-from-render preflight that detects an existing upload and refuses unless forced; add a pre-upload remaining-quota check + an upload-failure alert; and stop retry minting a second production. Verify against the real Pentimento channel (clean up the 2 orphan productions first).",
  },
  {
    key: "publication-record-reconciliation",
    title: "Publication records diverge from YouTube (#84) — duplicate-guard hardened; YouTube→platform discovery deferred",
    ticket: "01KYTR4TQ1XMYDTFTB5YDSD52B",
    status: "shipped_pending_verification",
    summary:
      "#84: the platform's model of what's live diverged from YouTube three ways (a live top video with NO record, a duplicate live, per-video counts disagreeing). SHIPPED (2026-07-31) the recurrence-prevention: the duplicate-publish guard now RE-RUNS immediately before the upload (production-pipeline.ts, right before the videos.insert), not only at pipeline start. The start-of-pipeline guard is a TOCTOU race — it reads a sibling's providerVideoId which is only written AFTER that sibling uploads, so two concurrent runs for one idea both passed it and both shipped (the two-live-Krypton case). The late re-check collapses the race window from the whole pipeline to the few steps between check and record-provider-video-id. DEFERRED (operator-live, sandbox-unverifiable): (1) the YouTube→platform DISCOVERY direction — reconcile_publications only verifies rows it already holds, so a live video the platform never produced (the 'Helium' top performer) is invisible; adopting it means minting synthetic production/publication records from external state, which writes prod data and needs the operator present. The provider already lists a channel's uploads inside findRecentUpload (channels.list uploads-playlist → playlistItems.list), the natural foundation. (2) A DB-level unique backstop (publications has no uniqueness on idea/video) to fully close the race — needs a denormalised ideaId + partial unique index migration. Typecheck + build verified; the guard's helper (publishedVideoForIdea) is already unit-tested.",
    nextStep:
      "With the operator present: add a provider listChannelUploads + a reconcile_publications discovery mode that REPORTS (then, opt-in, ADOPTS) live videos with no platform row; and add the ideaId + partial-unique-index migration as the hard duplicate backstop. Verify against the real Atom & Friends channel (Helium orphan + duplicate Krypton).",
  },
  {
    key: "analytics-video-zeros",
    title: "Per-video analytics read zeros where Studio has data (#17) — coverage now honest; ingest root deferred",
    ticket: "01KY1VEZ094TRVH8G06JX4MJVR",
    status: "shipped_pending_verification",
    summary:
      "#17 (latest appends): a published video read avgViewDuration/watchTime/subsGained = 0 where Studio shows real data, and coverage claimed watchPct/watchTime/engagement:true while returning 0 (a hard 0 that flows into channelAvgViewPct/vsChannelAvgPct is worse than a null). SHIPPED (2026-07-31) the coverage HONESTY fix: analyticsCoverage() (pure, unit-tested) now treats a watch metric of 0 on a video WITH views as not-yet-ingested — watchPct/watchTime/impressionsCtr require a value > 0, an empty retention array isn't coverage, and dataState reports 'pending' instead of 'partial/full' on a zero-watch snapshot. So the misleading 0s now correctly read as pending/uncovered (the operator's core complaint). DEFERRED (needs live Analytics API to diagnose, sandbox can't): WHY the video-level report returns 0 for watch/subs metrics while Studio shows non-zero — the provider mapping (analytics.ts: subscribersGained requested + mapped correctly, no hardcoded 0) is correct, so a stored 0 means the report returned a 0 row, pointing at a date-window or video-level-aggregation issue, not a mapping bug. Touching the fetch speculatively risks breaking working metrics. subsGained may also be downstream of #84 (a publication pointing at the wrong providerVideoId).",
    nextStep:
      "With the operator present (live Analytics API): diagnose the video-level report returning 0 for AVD/watch-time/subsGained on a video Studio shows data for — check the startDate/endDate window and whether isolating subscribersGained into its own report helps; reconcile the channel's records first (#84) in case a wrong providerVideoId is the cause.",
  },
  {
    key: "shot-pixel-dimensions",
    title: "Shot dimensions — true served pixel width/height capture",
    ticket: "01KY9EBKZ5T0MVT6JJRYDJ4ZQW",
    status: "deferred",
    summary:
      "SHIPPED the aspect-observability slice of #50: generated stills already inherit the production aspect (the videoAspect 'one rule'), and now regenerate_shot RECORDS that aspect on the shot + accepts an aspectRatio override, while get_production_shots + get_gate report renderAspect, per-shot aspect, aspectMismatchShots and shotsWithUnknownAspect. This surfaces the REQUESTED/recorded render aspect, not the DECODED pixel width/height of the served image — so an engine that ignores the aspect param and returns a portrait PNG for a 16:9 request is still not caught for shots generated before this landed. DEFERRED: decode true served dimensions (sharp is already a providers dep; the buffer is in hand in each adapter before upload), return width/height from MediaProvider.generateImage, and persist them at every generation site (worker pipeline + shot-ops) so aspectMismatch is pixel-true and older shots are backfillable.",
    nextStep:
      "Add width/height to the MediaProvider.generateImage return, decode via sharp in each real adapter + the mock, and persist to shot meta at the worker pipeline image-write sites and shot-ops. Then flag aspectMismatch on decoded pixels, not recorded aspect. Verify against a real generation (the sandbox is mock-only).",
  },
  {
    key: "media-library-epic",
    title: "Media asset library — variation-controlled reuse",
    ticket: "GitHub #26",
    status: "deferred",
    summary:
      "Store every image/clip with tags + license + useCount + lastUsedAt + perceptualHash; retrieve relevant+fresh+unused assets before sourcing/generating. Framed as variation-CONTROLLED reuse (deprioritise heavy/recent use; never repeat hero shots in consecutive videos) so it's a compliance asset, not a liability. Absorbs the cross-production image dedup.",
    nextStep: "Spec at GitHub #26. Sequence after analytics + reconciliation are verified live; get operator sign-off on the schema + freshness policy first.",
  },
];

export function deferredByStatus(status: DeferredStatus): DeferredItem[] {
  return DEFERRED_WORK.filter((d) => d.status === status);
}
